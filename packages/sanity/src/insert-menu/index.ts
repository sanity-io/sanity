/**
 * Forked from `@sanity/insert-menu` (sanity-io/visual-editing). The app frontend ships its own
 * copy in `@sanity/visual-editing` (`packages/visual-editing/src/ui/insert-menu`, see
 * https://github.com/sanity-io/visual-editing/pull/3490) rendered when previewing through
 * `sanity/presentation` — keep the two implementations (and the `InsertMenuOptions` type in
 * `@sanity/types` / `@sanity/visual-editing-types`) in sync.
 */
export * from './InsertMenu'
