/**
 * Forked from `@sanity/insert-menu` (sanity-io/visual-editing). The app frontend ships its own
 * copy of this component in `@sanity/visual-editing` (`packages/visual-editing/src/ui/insert-menu`,
 * see https://github.com/sanity-io/visual-editing/pull/3490) rendered when previewing through
 * `sanity/presentation` — keep the two implementations in sync. The `InsertMenuOptions` type is
 * single-sourced from `@sanity/types`.
 */
export * from './InsertMenu'
