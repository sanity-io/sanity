/* eslint-disable i18next/no-literal-string */
import {Stack, Text} from '@sanity/ui'

export function TasksLayout() {
  return (
    <Stack padding={4} paddingTop={5} space={4}>
      <Text>Display a list of tasks and allow the user to interact with them.</Text>
    </Stack>
  )
}
