/**
 * Sentinel value used in resolved pane collections while a pane is still
 * resolving.
 *
 * @internal
 */
export const LOADING_PANE = Symbol('LOADING_PANE')

/**
 * The name of the portal element the document panel renders for pane-level
 * dialogs and menus.
 *
 * @internal
 */
export const DOCUMENT_PANEL_PORTAL_ELEMENT = 'documentPanelPortalElement'
