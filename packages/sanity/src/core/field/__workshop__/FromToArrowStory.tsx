import {Box} from '@sanity/ui'

import {FromToArrow} from '../diff'

export default function FromToArrowStory() : React.JSX.Element {
  return (
    <Box padding={4}>
      <FromToArrow />
    </Box>
  )
}
