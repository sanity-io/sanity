import {style} from '@vanilla-extract/css'

export const codeWrapper = style({
  overflowX: 'auto',
  position: 'relative',
})

export const meta = style({
  position: 'absolute',
  top: 0,
  right: 0,
})
