import {type BundleDocument} from './useBundleDocuments'

/**
 * A single document in a release, along with its validation status.
 * Returned by {@link useReleaseDocuments}.
 *
 * @public
 */
export type DocumentInRelease = BundleDocument
