{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": [
        "lib/**"
      ]
    },
    "build:bundle": {
      "outputs": [
        "dist/**"
      ]
    },
    "build:typedoc": {
      "outputs": [
        "typedoc/**"
      ]
    }
  }
}
