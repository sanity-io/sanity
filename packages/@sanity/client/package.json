{
  "name": "@sanity/client",
  "version": "0.106.16",
  "description": "Client for retrieving data from Sanity",
  "main": "lib/sanityClient.js",
  "umd": "umd/sanityClient.min.js",
  "scripts": {
    "browserify": "NODE_ENV=production BROWSERIFY_ENV=build DEBUG='' browserify -t envify -g uglifyify lib/sanityClient.js -o umd/sanityClient.js --standalone=SanityClient",
    "build": "npm run browserify && npm run minify && npm run size",
    "size": "node -r babel-register src/scripts/print-bundle-size",
    "clean": "rimraf lib coverage .nyc_output umd/*.js",
    "coverage": "DEBUG=sanity NODE_ENV=test nyc --reporter=html --reporter=lcov --reporter=text npm test",
    "minify": "uglifyjs -c -m -- umd/sanityClient.js > umd/sanityClient.min.js",
    "posttest": "eslint .",
    "prepublish": "in-publish && npm run build || not-in-publish",
    "test": "NODE_ENV=test tape -r babel-register test/*.test.js"
  },
  "dependencies": {
    "@sanity/eventsource": "^0.106.13",
    "@sanity/observable": "^0.106.16",
    "create-error-class": "^3.0.2",
    "deep-assign": "^2.0.0",
    "get-it": "^1.0.3",
    "in-publish": "^2.0.0",
    "object-assign": "^4.1.1"
  },
  "devDependencies": {
    "boxen": "^1.0.0",
    "browserify": "^14.3.0",
    "chalk": "^1.1.3",
    "envify": "^4.0.0",
    "eslint": "^3.19.0",
    "eslint-config-sanity": "^2.1.4",
    "gzip-size": "^3.0.0",
    "hard-rejection": "^1.0.0",
    "nock": "^9.0.5",
    "nyc": "^10.1.2",
    "pretty-bytes": "^4.0.2",
    "rimraf": "^2.6.1",
    "sse-channel": "^2.0.6",
    "tape": "^4.6.2",
    "uglify-js": "^3.0.1",
    "uglifyify": "^3.0.4"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/sanity-io/sanity.git"
  },
  "keywords": [
    "sanity",
    "client",
    "fetch",
    "api",
    "gradient"
  ],
  "author": "Sanity.io <hello@sanity.io>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/sanity-io/sanity/issues"
  },
  "homepage": "https://github.com/sanity-io/sanity/tree/master/packages/%40sanity/client",
  "nyc": {
    "include": [
      "src/**/*.js"
    ],
    "require": [
      "babel-register"
    ],
    "sourceMap": false,
    "instrument": false
  }
}
