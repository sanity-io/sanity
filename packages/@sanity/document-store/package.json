{
  "name": "@sanity/document-store",
  "version": "0.104.10",
  "description": "",
  "main": "./lib/index.js",
  "scripts": {
    "clean": "rimraf lib",
    "examples": "echo 'Running examples'; for i in examples/*.js; do echo '\n## '$i' ----\n' && node $i; done",
    "tap": "tap --node-arg=--require --node-arg=babel-register test/*.test.js",
    "test": "npm run tap && npm run examples"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@sanity/mutator": "^0.104.10",
    "@sanity/observable": "^0.103.13",
    "lodash": "^4.17.4",
    "nano-pubsub": "^1.0.1"
  },
  "devDependencies": {
    "get-random-values": "^1.2.0",
    "in-publish": "^2.0.0",
    "rimraf": "^2.6.1",
    "tap": "^9.0.3"
  }
}
