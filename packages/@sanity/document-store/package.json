{
  "name": "@sanity/document-store",
  "version": "0.2.3",
  "description": "",
  "main": "./lib/index.js",
  "scripts": {
    "compile": "babel src --copy-files --out-dir lib",
    "prepublish": "in-publish && npm run compile || not-in-publish",
    "postpublish": "rimraf lib",
    "examples": "echo 'Running examples'; for i in examples/*.js; do echo '\n## '$i' ----\n' && node $i; done",
    "tap": "tap --node-arg=--require --node-arg=babel-register test/*.test.js",
    "test": "npm run tap && npm run examples"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@sanity/mutator": "^0.1.6",
    "@sanity/observable": "^1.3.1",
    "lodash": "^4.17.2",
    "nano-pubsub": "^1.0.1"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "eslint": "^3.12.1",
    "eslint-config-sanity": "^2.0.1",
    "get-random-values": "^1.2.0",
    "in-publish": "^2.0.0",
    "rimraf": "^2.5.4",
    "tap": "^8.0.1"
  }
}
