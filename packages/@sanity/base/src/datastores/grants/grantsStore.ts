// note: this needs to be in a separate file (as opposed to in index) to prevent
// circular dependencies in jest
import {createGrantsStore} from './createGrantsStore'

export default createGrantsStore()
