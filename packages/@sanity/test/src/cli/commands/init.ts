/* eslint-disable no-console */

export function initCommand(): void {
  /**
   * Check if the config file exists
   * If not maybe run the playwright init command or ask them to run it
   */
  /**
   * If the config file exists, update the config file to include the new config from the package
   * Things to consider.
   *  1. What if they have default config already?
   *    Maybe we move the config to a new file `playwright.config.bkp.ts` and provide information about copying config
   */

  console.log('Init command')
}
