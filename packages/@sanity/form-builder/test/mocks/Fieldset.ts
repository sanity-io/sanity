//@ts-expect-error (it's ok, the path actually exists after sanity build, it's just not shipped with types)
export {default} from '@sanity/base/lib/__legacy/@sanity/components/fieldsets/DefaultFieldset'
