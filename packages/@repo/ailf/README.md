# @repo/ailf

AI Literacy Framework (AILF) evaluation configuration for this repository.

Evaluations run automatically via `.github/workflows/ailf-eval.yml` on PRs
touching this package, on a weekly schedule, and via manual dispatch.
