# The API token is the only env-var configuration — everything else is CLI
# flags (run with --help for the full list).
SANITY_TOKEN=your-token  # Injected as Authorization: Bearer (required for write operations or private datasets when using cookie-based auth)
