{
  // Until what's in dev/, scripts. and test/ is in workspaces it's necessary to type check them from the root
  "extends": "@repo/tsconfig/check.json",
  "include": ["./examples/studios", "./scripts", "./test"],
  "files": [],
  "compilerOptions": {
    "rootDir": ".",
    "module": "esnext"
  }
}
