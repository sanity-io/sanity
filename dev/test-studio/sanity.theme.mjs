// Generated 2023-08-10T00:53:46.713Z by https://themer.sanity.build/api/hues?preset=tw-cyan&default=64748b&primary=d946ef%3Blightest%3Afdf4ff%3Bdarkest%3A701a75&transparent=6b7180%3Bdarkest%3A111826&positive=43d675%3B400%3Blightest%3Af8fafc&caution=f59e09%3B300%3Blightest%3Afffbeb%3Bdarkest%3A783510&critical=f43f5e%3Blightest%3Afef1f2%3Bdarkest%3A881337&lightest=ffffff&darkest=0f172a&min=0
// Not minified, remove `?min=0` from the request for much smaller output

// ../../node_modules/.pnpm/@sanity+ui@1.7.4_react-dom@18.2.0_react-is@18.2.0_react@18.2.0_styled-components@5.3.11/node_modules/
var fonts = {
  code: {
    family: 'ui-monospace, SFMono-Regular, SF Mono, Menlo, Consolas, Liberation Mono, monospace',
    weights: {
      regular: 400,
      medium: 500,
      semibold: 600,
      bold: 700,
    },
    sizes: [
      {
        ascenderHeight: 3,
        descenderHeight: 3,
        fontSize: 10,
        iconSize: 17,
        lineHeight: 13,
        letterSpacing: 0,
      },
      {
        ascenderHeight: 4,
        descenderHeight: 4,
        fontSize: 13,
        iconSize: 21,
        lineHeight: 17,
        letterSpacing: 0,
      },
      {
        ascenderHeight: 5,
        descenderHeight: 5,
        fontSize: 16,
        iconSize: 25,
        lineHeight: 21,
        letterSpacing: 0,
      },
      {
        ascenderHeight: 6,
        descenderHeight: 6,
        fontSize: 19,
        iconSize: 29,
        lineHeight: 25,
        letterSpacing: 0,
      },
      {
        ascenderHeight: 7,
        descenderHeight: 7,
        fontSize: 22,
        iconSize: 33,
        lineHeight: 29,
        letterSpacing: 0,
      },
    ],
  },
  heading: {
    family:
      '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", "Liberation Sans", Helvetica, Arial, system-ui, sans-serif',
    weights: {
      regular: 700,
      medium: 800,
      semibold: 900,
      bold: 900,
    },
    sizes: [
      {
        ascenderHeight: 4,
        descenderHeight: 4,
        fontSize: 12,
        iconSize: 17,
        lineHeight: 17,
        letterSpacing: 0,
      },
      {
        ascenderHeight: 5,
        descenderHeight: 5,
        fontSize: 16,
        iconSize: 25,
        lineHeight: 21,
        letterSpacing: 0,
      },
      {
        ascenderHeight: 6,
        descenderHeight: 6,
        fontSize: 21,
        iconSize: 33,
        lineHeight: 27,
        letterSpacing: 0,
      },
      {
        ascenderHeight: 7,
        descenderHeight: 7,
        fontSize: 27,
        iconSize: 41,
        lineHeight: 33,
        letterSpacing: 0,
      },
      {
        ascenderHeight: 8,
        descenderHeight: 8,
        fontSize: 33,
        iconSize: 49,
        lineHeight: 39,
        letterSpacing: 0,
      },
      {
        ascenderHeight: 9,
        descenderHeight: 9,
        fontSize: 38,
        iconSize: 53,
        lineHeight: 45,
        letterSpacing: 0,
      },
    ],
  },
  label: {
    family:
      '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", "Liberation Sans", system-ui, sans-serif',
    weights: {
      regular: 600,
      medium: 700,
      semibold: 800,
      bold: 900,
    },
    sizes: [
      {
        ascenderHeight: 2,
        descenderHeight: 2,
        fontSize: 9.8,
        iconSize: 15,
        lineHeight: 11,
        letterSpacing: 0.5,
      },
      {
        ascenderHeight: 2,
        descenderHeight: 2,
        fontSize: 11.25,
        iconSize: 17,
        lineHeight: 12,
        letterSpacing: 0.5,
      },
      {
        ascenderHeight: 2,
        descenderHeight: 2,
        fontSize: 12.75,
        iconSize: 19,
        lineHeight: 13,
        letterSpacing: 0.5,
      },
      {
        ascenderHeight: 2,
        descenderHeight: 2,
        fontSize: 14,
        iconSize: 21,
        lineHeight: 14,
        letterSpacing: 0.5,
      },
      {
        ascenderHeight: 2,
        descenderHeight: 2,
        fontSize: 15.5,
        iconSize: 23,
        lineHeight: 15,
        letterSpacing: 0.5,
      },
    ],
  },
  text: {
    family:
      '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", "Liberation Sans", Helvetica, Arial, system-ui, sans-serif',
    weights: {
      regular: 400,
      medium: 500,
      semibold: 600,
      bold: 700,
    },
    sizes: [
      {
        ascenderHeight: 3,
        descenderHeight: 3,
        fontSize: 10,
        iconSize: 17,
        lineHeight: 13,
        letterSpacing: 0,
      },
      {
        ascenderHeight: 4,
        descenderHeight: 4,
        fontSize: 13,
        iconSize: 21,
        lineHeight: 17,
        letterSpacing: 0,
      },
      {
        ascenderHeight: 5,
        descenderHeight: 5,
        fontSize: 16,
        iconSize: 25,
        lineHeight: 21,
        letterSpacing: 0,
      },
      {
        ascenderHeight: 6,
        descenderHeight: 6,
        fontSize: 19,
        iconSize: 29,
        lineHeight: 25,
        letterSpacing: 0,
      },
      {
        ascenderHeight: 7,
        descenderHeight: 7,
        fontSize: 22,
        iconSize: 33,
        lineHeight: 29,
        letterSpacing: 0,
      },
    ],
  },
}

// ../../node_modules/.pnpm/@sanity+ui@1.7.4_react-dom@18.2.0_react-is@18.2.0_react@18.2.0_styled-components@5.3.11/node_modules/@sanity/ui/src/theme/studioTheme/theme.ts
var studioTheme = {
  avatar: {
    sizes: [
      {distance: -3, size: 25},
      {distance: -6, size: 35},
      {distance: -9, size: 55},
    ],
  },
  button: {
    textWeight: 'medium',
  },
  container: [320, 640, 960, 1280, 1600, 1920],
  focusRing: {
    offset: 1,
    width: 2,
  },
  fonts,
  media: [360, 600, 900, 1200, 1800, 2400],
  radius: [0, 1, 3, 6, 9, 12, 21],
  shadows: [
    null,
    {umbra: [0, 0, 0, 0], penumbra: [0, 0, 0, 0], ambient: [0, 0, 0, 0]},
    {umbra: [0, 3, 5, -1], penumbra: [0, 6, 10, 0], ambient: [0, 1, 18, 0]},
    {umbra: [0, 7, 8, -4], penumbra: [0, 12, 17, 2], ambient: [0, 5, 22, 4]},
    {umbra: [0, 9, 11, -5], penumbra: [0, 18, 28, 2], ambient: [0, 7, 34, 6]},
    {umbra: [0, 11, 15, -7], penumbra: [0, 24, 38, 3], ambient: [0, 9, 46, 8]},
  ],
  space: [0, 4, 8, 12, 20, 32, 52, 84, 136, 220],
  input: {
    checkbox: {
      size: 17,
    },
    radio: {
      size: 17,
      markSize: 9,
    },
    switch: {
      width: 33,
      height: 17,
      padding: 4,
      transitionDurationMs: 150,
      transitionTimingFunction: 'ease-out',
    },
    border: {
      width: 1,
    },
  },
  // styles: {
  //   button: {
  //     root: {
  //       transition: 'background-color 100ms,border-color 100ms,color 100ms',
  //     },
  //   },
  //   card: {
  //     root: {
  //       transition: 'background-color 100ms,border-color 100ms,color 100ms',
  //     },
  //   },
  // },
}

// ../../node_modules/.pnpm/@sanity+color@2.2.5/node_modules/@sanity/color/dist/index.js
var black$1 = '#101112',
  white$1 = '#fff',
  gray = {
    50: {
      title: 'Gray 50',
      hex: '#f2f3f5',
    },
    100: {
      title: 'Gray 100',
      hex: '#e6e8ec',
    },
    200: {
      title: 'Gray 200',
      hex: '#ced2d9',
    },
    300: {
      title: 'Gray 300',
      hex: '#b6bcc6',
    },
    400: {
      title: 'Gray 400',
      hex: '#9ea6b3',
    },
    500: {
      title: 'Gray 500',
      hex: '#8690a0',
    },
    600: {
      title: 'Gray 600',
      hex: '#6e7683',
    },
    700: {
      title: 'Gray 700',
      hex: '#565d67',
    },
    800: {
      title: 'Gray 800',
      hex: '#3f434a',
    },
    900: {
      title: 'Gray 900',
      hex: '#272a2e',
    },
    950: {
      title: 'Gray 950',
      hex: '#1b1d20',
    },
  },
  blue = {
    50: {
      title: 'Blue 50',
      hex: '#e8f1fe',
    },
    100: {
      title: 'Blue 100',
      hex: '#d2e3fe',
    },
    200: {
      title: 'Blue 200',
      hex: '#a6c8fd',
    },
    300: {
      title: 'Blue 300',
      hex: '#7aacfd',
    },
    400: {
      title: 'Blue 400',
      hex: '#4e91fc',
    },
    500: {
      title: 'Blue 500',
      hex: '#2276fc',
    },
    600: {
      title: 'Blue 600',
      hex: '#1e61cd',
    },
    700: {
      title: 'Blue 700',
      hex: '#1a4d9e',
    },
    800: {
      title: 'Blue 800',
      hex: '#17396f',
    },
    900: {
      title: 'Blue 900',
      hex: '#132540',
    },
    950: {
      title: 'Blue 950',
      hex: '#111b29',
    },
  },
  purple = {
    50: {
      title: 'Purple 50',
      hex: '#f8e9fe',
    },
    100: {
      title: 'Purple 100',
      hex: '#f2d3fe',
    },
    200: {
      title: 'Purple 200',
      hex: '#e6a7fd',
    },
    300: {
      title: 'Purple 300',
      hex: '#d97bfd',
    },
    400: {
      title: 'Purple 400',
      hex: '#cd4efc',
    },
    500: {
      title: 'Purple 500',
      hex: '#c123fc',
    },
    600: {
      title: 'Purple 600',
      hex: '#9d1fcd',
    },
    700: {
      title: 'Purple 700',
      hex: '#7a1b9e',
    },
    800: {
      title: 'Purple 800',
      hex: '#56186f',
    },
    900: {
      title: 'Purple 900',
      hex: '#331440',
    },
    950: {
      title: 'Purple 950',
      hex: '#211229',
    },
  },
  magenta = {
    50: {
      title: 'Magenta 50',
      hex: '#fcebf5',
    },
    100: {
      title: 'Magenta 100',
      hex: '#f9d7eb',
    },
    200: {
      title: 'Magenta 200',
      hex: '#f4afd8',
    },
    300: {
      title: 'Magenta 300',
      hex: '#ef87c4',
    },
    400: {
      title: 'Magenta 400',
      hex: '#ea5fb1',
    },
    500: {
      title: 'Magenta 500',
      hex: '#e5389e',
    },
    600: {
      title: 'Magenta 600',
      hex: '#ba3082',
    },
    700: {
      title: 'Magenta 700',
      hex: '#8f2866',
    },
    800: {
      title: 'Magenta 800',
      hex: '#65204a',
    },
    900: {
      title: 'Magenta 900',
      hex: '#3a182d',
    },
    950: {
      title: 'Magenta 950',
      hex: '#25141f',
    },
  },
  red = {
    50: {
      title: 'Red 50',
      hex: '#fdebea',
    },
    100: {
      title: 'Red 100',
      hex: '#fcd8d5',
    },
    200: {
      title: 'Red 200',
      hex: '#f9b1ab',
    },
    300: {
      title: 'Red 300',
      hex: '#f68b82',
    },
    400: {
      title: 'Red 400',
      hex: '#f36458',
    },
    500: {
      title: 'Red 500',
      hex: '#f03e2f',
    },
    600: {
      title: 'Red 600',
      hex: '#c33529',
    },
    700: {
      title: 'Red 700',
      hex: '#962c23',
    },
    800: {
      title: 'Red 800',
      hex: '#69231d',
    },
    900: {
      title: 'Red 900',
      hex: '#3c1a17',
    },
    950: {
      title: 'Red 950',
      hex: '#261514',
    },
  },
  orange = {
    50: {
      title: 'Orange 50',
      hex: '#fef0e6',
    },
    100: {
      title: 'Orange 100',
      hex: '#fee2ce',
    },
    200: {
      title: 'Orange 200',
      hex: '#fdc59d',
    },
    300: {
      title: 'Orange 300',
      hex: '#fca86d',
    },
    400: {
      title: 'Orange 400',
      hex: '#fb8b3c',
    },
    500: {
      title: 'Orange 500',
      hex: '#e57322',
    },
    600: {
      title: 'Orange 600',
      hex: '#ba5f1f',
    },
    700: {
      title: 'Orange 700',
      hex: '#904b1b',
    },
    800: {
      title: 'Orange 800',
      hex: '#653818',
    },
    900: {
      title: 'Orange 900',
      hex: '#3a2415',
    },
    950: {
      title: 'Orange 950',
      hex: '#251a13',
    },
  },
  yellow = {
    50: {
      title: 'Yellow 50',
      hex: '#fef7da',
    },
    100: {
      title: 'Yellow 100',
      hex: '#fdefb6',
    },
    200: {
      title: 'Yellow 200',
      hex: '#fcdf6d',
    },
    300: {
      title: 'Yellow 300',
      hex: '#fbd024',
    },
    400: {
      title: 'Yellow 400',
      hex: '#d9b421',
    },
    500: {
      title: 'Yellow 500',
      hex: '#b7991e',
    },
    600: {
      title: 'Yellow 600',
      hex: '#967e1c',
    },
    700: {
      title: 'Yellow 700',
      hex: '#746219',
    },
    800: {
      title: 'Yellow 800',
      hex: '#534717',
    },
    900: {
      title: 'Yellow 900',
      hex: '#312c14',
    },
    950: {
      title: 'Yellow 950',
      hex: '#201e13',
    },
  },
  green = {
    50: {
      title: 'Green 50',
      hex: '#e7f9ed',
    },
    100: {
      title: 'Green 100',
      hex: '#d0f4dc',
    },
    200: {
      title: 'Green 200',
      hex: '#a1eaba',
    },
    300: {
      title: 'Green 300',
      hex: '#72e097',
    },
    400: {
      title: 'Green 400',
      hex: '#43d675',
    },
    500: {
      title: 'Green 500',
      hex: '#3ab564',
    },
    600: {
      title: 'Green 600',
      hex: '#329454',
    },
    700: {
      title: 'Green 700',
      hex: '#297343',
    },
    800: {
      title: 'Green 800',
      hex: '#215233',
    },
    900: {
      title: 'Green 900',
      hex: '#183122',
    },
    950: {
      title: 'Green 950',
      hex: '#14211a',
    },
  },
  cyan = {
    50: {
      title: 'Cyan 50',
      hex: '#e3fafd',
    },
    100: {
      title: 'Cyan 100',
      hex: '#c7f5fc',
    },
    200: {
      title: 'Cyan 200',
      hex: '#90ecf9',
    },
    300: {
      title: 'Cyan 300',
      hex: '#59e3f6',
    },
    400: {
      title: 'Cyan 400',
      hex: '#22daf4',
    },
    500: {
      title: 'Cyan 500',
      hex: '#1fb8ce',
    },
    600: {
      title: 'Cyan 600',
      hex: '#1c97a8',
    },
    700: {
      title: 'Cyan 700',
      hex: '#197583',
    },
    800: {
      title: 'Cyan 800',
      hex: '#16545d',
    },
    900: {
      title: 'Cyan 900',
      hex: '#133237',
    },
    950: {
      title: 'Cyan 950',
      hex: '#112124',
    },
  },
  hues = {
    gray,
    blue,
    purple,
    magenta,
    red,
    orange,
    yellow,
    green,
    cyan,
  }
var COLOR_TINTS = ['50', '100', '200', '300', '400', '500', '600', '700', '800', '900', '950'],
  black = {
    title: 'Black',
    hex: black$1,
  },
  white = {
    title: 'White',
    hex: white$1,
  }

// utils/colors.ts
var lightest = white.hex.toLowerCase(),
  darkest = black.hex.toLowerCase(),
  NEUTRAL_TONES = ['default', 'transparent']
var {
  blue: blue2,
  cyan: cyan2,
  gray: gray2,
  green: green2,
  magenta: magenta2,
  orange: orange2,
  purple: purple2,
  red: red2,
  yellow: yellow2,
} = hues

// utils/applyHues.ts
var defaultHues = {
  default: {
    lightest,
    darkest,
    mid: '#8690a0',
    midPoint: 500,
  },
  primary: {
    lightest,
    darkest,
    mid: '#2276fc',
    midPoint: 500,
  },
  transparent: {
    lightest,
    darkest,
    mid: '#8690a0',
    midPoint: 500,
  },
  positive: {
    lightest,
    darkest,
    mid: '#43d675',
    midPoint: 400,
  },
  caution: {
    lightest,
    darkest,
    mid: '#fbd024',
    midPoint: 300,
  },
  critical: {
    lightest,
    darkest,
    mid: '#f03e2f',
    midPoint: 500,
  },
}
function applyHues(_hues, defaults = defaultHues) {
  var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l
  let hues3 = JSON.parse(JSON.stringify(_hues)),
    defaultMid =
      ((_b = (_a = hues3.default) == null ? void 0 : _a.mid) == null ? void 0 : _b.toLowerCase()) ||
      defaults.default.mid,
    primaryMid =
      ((_d = (_c = hues3.primary) == null ? void 0 : _c.mid) == null ? void 0 : _d.toLowerCase()) ||
      defaults.primary.mid,
    transparentMid =
      ((_f = (_e = hues3.transparent) == null ? void 0 : _e.mid) == null
        ? void 0
        : _f.toLowerCase()) || defaults.transparent.mid,
    positiveMid =
      ((_h = (_g = hues3.positive) == null ? void 0 : _g.mid) == null
        ? void 0
        : _h.toLowerCase()) || defaults.positive.mid,
    cautionMid =
      ((_j = (_i = hues3.caution) == null ? void 0 : _i.mid) == null ? void 0 : _j.toLowerCase()) ||
      defaults.caution.mid,
    criticalMid =
      ((_l = (_k = hues3.critical) == null ? void 0 : _k.mid) == null
        ? void 0
        : _l.toLowerCase()) || defaults.critical.mid
  return {
    default: {
      ...defaults.default,
      midPoint: defaultMid === defaults.default.mid ? defaults.default.midPoint : 500,
      ...hues3.default,
      mid: defaultMid,
    },
    primary: {
      ...defaults.primary,
      midPoint: primaryMid === defaults.primary.mid ? defaults.primary.midPoint : 500,
      ...hues3.primary,
      mid: primaryMid,
    },
    transparent: {
      ...defaults.transparent,
      midPoint: transparentMid === defaults.transparent.mid ? defaults.transparent.midPoint : 500,
      ...hues3.transparent,
      mid: transparentMid,
    },
    positive: {
      ...defaults.positive,
      midPoint: positiveMid === defaults.positive.mid ? defaults.positive.midPoint : 500,
      ...hues3.positive,
      mid: positiveMid,
    },
    caution: {
      ...defaults.caution,
      midPoint: cautionMid === defaults.caution.mid ? defaults.caution.midPoint : 500,
      ...hues3.caution,
      mid: cautionMid,
    },
    critical: {
      ...defaults.critical,
      midPoint: criticalMid === defaults.critical.mid ? defaults.critical.midPoint : 500,
      ...hues3.critical,
      mid: criticalMid,
    },
  }
}

// ../../node_modules/.pnpm/@babel+runtime@7.21.0/node_modules/@babel/runtime/helpers/esm/extends.js
function _extends() {
  return (
    (_extends = Object.assign
      ? Object.assign.bind()
      : function (target) {
          for (var i = 1; i < arguments.length; i++) {
            var source = arguments[i]
            for (var key in source)
              Object.prototype.hasOwnProperty.call(source, key) && (target[key] = source[key])
          }
          return target
        }),
    _extends.apply(this, arguments)
  )
}

// ../../node_modules/.pnpm/@babel+runtime@7.21.0/node_modules/@babel/runtime/helpers/esm/assertThisInitialized.js
function _assertThisInitialized(self) {
  if (self === void 0)
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called")
  return self
}

// ../../node_modules/.pnpm/@babel+runtime@7.21.0/node_modules/@babel/runtime/helpers/esm/setPrototypeOf.js
function _setPrototypeOf(o, p) {
  return (
    (_setPrototypeOf = Object.setPrototypeOf
      ? Object.setPrototypeOf.bind()
      : function (o2, p2) {
          return (o2.__proto__ = p2), o2
        }),
    _setPrototypeOf(o, p)
  )
}

// ../../node_modules/.pnpm/@babel+runtime@7.21.0/node_modules/@babel/runtime/helpers/esm/inheritsLoose.js
function _inheritsLoose(subClass, superClass) {
  ;(subClass.prototype = Object.create(superClass.prototype)),
    (subClass.prototype.constructor = subClass),
    _setPrototypeOf(subClass, superClass)
}

// ../../node_modules/.pnpm/@babel+runtime@7.21.0/node_modules/@babel/runtime/helpers/esm/getPrototypeOf.js
function _getPrototypeOf(o) {
  return (
    (_getPrototypeOf = Object.setPrototypeOf
      ? Object.getPrototypeOf.bind()
      : function (o2) {
          return o2.__proto__ || Object.getPrototypeOf(o2)
        }),
    _getPrototypeOf(o)
  )
}

// ../../node_modules/.pnpm/@babel+runtime@7.21.0/node_modules/@babel/runtime/helpers/esm/isNativeFunction.js
function _isNativeFunction(fn) {
  return Function.toString.call(fn).indexOf('[native code]') !== -1
}

// ../../node_modules/.pnpm/@babel+runtime@7.21.0/node_modules/@babel/runtime/helpers/esm/isNativeReflectConstruct.js
function _isNativeReflectConstruct() {
  if (typeof Reflect > 'u' || !Reflect.construct || Reflect.construct.sham) return !1
  if (typeof Proxy == 'function') return !0
  try {
    return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function () {})), !0
  } catch (e) {
    return !1
  }
}

// ../../node_modules/.pnpm/@babel+runtime@7.21.0/node_modules/@babel/runtime/helpers/esm/construct.js
function _construct(Parent, args, Class) {
  return (
    _isNativeReflectConstruct()
      ? (_construct = Reflect.construct.bind())
      : (_construct = function (Parent2, args2, Class2) {
          var a = [null]
          a.push.apply(a, args2)
          var Constructor = Function.bind.apply(Parent2, a),
            instance = new Constructor()
          return Class2 && _setPrototypeOf(instance, Class2.prototype), instance
        }),
    _construct.apply(null, arguments)
  )
}

// ../../node_modules/.pnpm/@babel+runtime@7.21.0/node_modules/@babel/runtime/helpers/esm/wrapNativeSuper.js
function _wrapNativeSuper(Class) {
  var _cache = typeof Map == 'function' ? /* @__PURE__ */ new Map() : void 0
  return (
    (_wrapNativeSuper = function (Class2) {
      if (Class2 === null || !_isNativeFunction(Class2)) return Class2
      if (typeof Class2 != 'function')
        throw new TypeError('Super expression must either be null or a function')
      if (typeof _cache < 'u') {
        if (_cache.has(Class2)) return _cache.get(Class2)
        _cache.set(Class2, Wrapper)
      }
      function Wrapper() {
        return _construct(Class2, arguments, _getPrototypeOf(this).constructor)
      }
      return (
        (Wrapper.prototype = Object.create(Class2.prototype, {
          constructor: {
            value: Wrapper,
            enumerable: !1,
            writable: !0,
            configurable: !0,
          },
        })),
        _setPrototypeOf(Wrapper, Class2)
      )
    }),
    _wrapNativeSuper(Class)
  )
}

// ../../node_modules/.pnpm/polished@4.2.2/node_modules/polished/dist/polished.esm.js
var ERRORS = {
  1: 'Passed invalid arguments to hsl, please pass multiple numbers e.g. hsl(360, 0.75, 0.4) or an object e.g. rgb({ hue: 255, saturation: 0.4, lightness: 0.75 }).\n\n',
  2: 'Passed invalid arguments to hsla, please pass multiple numbers e.g. hsla(360, 0.75, 0.4, 0.7) or an object e.g. rgb({ hue: 255, saturation: 0.4, lightness: 0.75, alpha: 0.7 }).\n\n',
  3: 'Passed an incorrect argument to a color function, please pass a string representation of a color.\n\n',
  4: "Couldn't generate valid rgb string from %s, it returned %s.\n\n",
  5: "Couldn't parse the color string. Please provide the color as a string in hex, rgb, rgba, hsl or hsla notation.\n\n",
  6: 'Passed invalid arguments to rgb, please pass multiple numbers e.g. rgb(255, 205, 100) or an object e.g. rgb({ red: 255, green: 205, blue: 100 }).\n\n',
  7: 'Passed invalid arguments to rgba, please pass multiple numbers e.g. rgb(255, 205, 100, 0.75) or an object e.g. rgb({ red: 255, green: 205, blue: 100, alpha: 0.75 }).\n\n',
  8: 'Passed invalid argument to toColorString, please pass a RgbColor, RgbaColor, HslColor or HslaColor object.\n\n',
  9: 'Please provide a number of steps to the modularScale helper.\n\n',
  10: 'Please pass a number or one of the predefined scales to the modularScale helper as the ratio.\n\n',
  11: 'Invalid value passed as base to modularScale, expected number or em string but got "%s"\n\n',
  12: 'Expected a string ending in "px" or a number passed as the first argument to %s(), got "%s" instead.\n\n',
  13: 'Expected a string ending in "px" or a number passed as the second argument to %s(), got "%s" instead.\n\n',
  14: 'Passed invalid pixel value ("%s") to %s(), please pass a value like "12px" or 12.\n\n',
  15: 'Passed invalid base value ("%s") to %s(), please pass a value like "12px" or 12.\n\n',
  16: 'You must provide a template to this method.\n\n',
  17: 'You passed an unsupported selector state to this method.\n\n',
  18: 'minScreen and maxScreen must be provided as stringified numbers with the same units.\n\n',
  19: 'fromSize and toSize must be provided as stringified numbers with the same units.\n\n',
  20: 'expects either an array of objects or a single object with the properties prop, fromSize, and toSize.\n\n',
  21: 'expects the objects in the first argument array to have the properties `prop`, `fromSize`, and `toSize`.\n\n',
  22: 'expects the first argument object to have the properties `prop`, `fromSize`, and `toSize`.\n\n',
  23: 'fontFace expects a name of a font-family.\n\n',
  24: 'fontFace expects either the path to the font file(s) or a name of a local copy.\n\n',
  25: 'fontFace expects localFonts to be an array.\n\n',
  26: 'fontFace expects fileFormats to be an array.\n\n',
  27: 'radialGradient requries at least 2 color-stops to properly render.\n\n',
  28: 'Please supply a filename to retinaImage() as the first argument.\n\n',
  29: "Passed invalid argument to triangle, please pass correct pointingDirection e.g. 'right'.\n\n",
  30: 'Passed an invalid value to `height` or `width`. Please provide a pixel based unit.\n\n',
  31: 'The animation shorthand only takes 8 arguments. See the specification for more information: http://mdn.io/animation\n\n',
  32: "To pass multiple animations please supply them in arrays, e.g. animation(['rotate', '2s'], ['move', '1s'])\nTo pass a single animation please supply them in simple values, e.g. animation('rotate', '2s')\n\n",
  33: 'The animation shorthand arrays can only have 8 elements. See the specification for more information: http://mdn.io/animation\n\n',
  34: 'borderRadius expects a radius value as a string or number as the second argument.\n\n',
  35: 'borderRadius expects one of "top", "bottom", "left" or "right" as the first argument.\n\n',
  36: 'Property must be a string value.\n\n',
  37: 'Syntax Error at %s.\n\n',
  38: 'Formula contains a function that needs parentheses at %s.\n\n',
  39: 'Formula is missing closing parenthesis at %s.\n\n',
  40: 'Formula has too many closing parentheses at %s.\n\n',
  41: 'All values in a formula must have the same unit or be unitless.\n\n',
  42: 'Please provide a number of steps to the modularScale helper.\n\n',
  43: 'Please pass a number or one of the predefined scales to the modularScale helper as the ratio.\n\n',
  44: 'Invalid value passed as base to modularScale, expected number or em/rem string but got %s.\n\n',
  45: 'Passed invalid argument to hslToColorString, please pass a HslColor or HslaColor object.\n\n',
  46: 'Passed invalid argument to rgbToColorString, please pass a RgbColor or RgbaColor object.\n\n',
  47: 'minScreen and maxScreen must be provided as stringified numbers with the same units.\n\n',
  48: 'fromSize and toSize must be provided as stringified numbers with the same units.\n\n',
  49: 'Expects either an array of objects or a single object with the properties prop, fromSize, and toSize.\n\n',
  50: 'Expects the objects in the first argument array to have the properties prop, fromSize, and toSize.\n\n',
  51: 'Expects the first argument object to have the properties prop, fromSize, and toSize.\n\n',
  52: 'fontFace expects either the path to the font file(s) or a name of a local copy.\n\n',
  53: 'fontFace expects localFonts to be an array.\n\n',
  54: 'fontFace expects fileFormats to be an array.\n\n',
  55: 'fontFace expects a name of a font-family.\n\n',
  56: 'linearGradient requries at least 2 color-stops to properly render.\n\n',
  57: 'radialGradient requries at least 2 color-stops to properly render.\n\n',
  58: 'Please supply a filename to retinaImage() as the first argument.\n\n',
  59: "Passed invalid argument to triangle, please pass correct pointingDirection e.g. 'right'.\n\n",
  60: 'Passed an invalid value to `height` or `width`. Please provide a pixel based unit.\n\n',
  61: 'Property must be a string value.\n\n',
  62: 'borderRadius expects a radius value as a string or number as the second argument.\n\n',
  63: 'borderRadius expects one of "top", "bottom", "left" or "right" as the first argument.\n\n',
  64: 'The animation shorthand only takes 8 arguments. See the specification for more information: http://mdn.io/animation.\n\n',
  65: "To pass multiple animations please supply them in arrays, e.g. animation(['rotate', '2s'], ['move', '1s'])\\nTo pass a single animation please supply them in simple values, e.g. animation('rotate', '2s').\n\n",
  66: 'The animation shorthand arrays can only have 8 elements. See the specification for more information: http://mdn.io/animation.\n\n',
  67: 'You must provide a template to this method.\n\n',
  68: 'You passed an unsupported selector state to this method.\n\n',
  69: 'Expected a string ending in "px" or a number passed as the first argument to %s(), got %s instead.\n\n',
  70: 'Expected a string ending in "px" or a number passed as the second argument to %s(), got %s instead.\n\n',
  71: 'Passed invalid pixel value %s to %s(), please pass a value like "12px" or 12.\n\n',
  72: 'Passed invalid base value %s to %s(), please pass a value like "12px" or 12.\n\n',
  73: 'Please provide a valid CSS variable.\n\n',
  74: 'CSS variable not found and no default was provided.\n\n',
  75: 'important requires a valid style object, got a %s instead.\n\n',
  76: 'fromSize and toSize must be provided as stringified numbers with the same units as minScreen and maxScreen.\n\n',
  77: 'remToPx expects a value in "rem" but you provided it in "%s".\n\n',
  78: 'base must be set in "px" or "%" but you set it in "%s".\n',
}
function format() {
  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++)
    args[_key] = arguments[_key]
  var a = args[0],
    b = [],
    c
  for (c = 1; c < args.length; c += 1) b.push(args[c])
  return (
    b.forEach(function (d) {
      a = a.replace(/%[a-z]/, d)
    }),
    a
  )
}
var PolishedError = /* @__PURE__ */ (function (_Error) {
  _inheritsLoose(PolishedError2, _Error)
  function PolishedError2(code) {
    for (
      var _this, _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1;
      _key2 < _len2;
      _key2++
    )
      args[_key2 - 1] = arguments[_key2]
    return (
      (_this = _Error.call(this, format.apply(void 0, [ERRORS[code]].concat(args))) || this),
      _assertThisInitialized(_this)
    )
  }
  return PolishedError2
})(/* @__PURE__ */ _wrapNativeSuper(Error))
function colorToInt(color) {
  return Math.round(color * 255)
}
function convertToInt(red3, green3, blue3) {
  return colorToInt(red3) + ',' + colorToInt(green3) + ',' + colorToInt(blue3)
}
function hslToRgb(hue, saturation, lightness, convert) {
  if ((convert === void 0 && (convert = convertToInt), saturation === 0))
    return convert(lightness, lightness, lightness)
  var huePrime = (((hue % 360) + 360) % 360) / 60,
    chroma = (1 - Math.abs(2 * lightness - 1)) * saturation,
    secondComponent = chroma * (1 - Math.abs((huePrime % 2) - 1)),
    red3 = 0,
    green3 = 0,
    blue3 = 0
  huePrime >= 0 && huePrime < 1
    ? ((red3 = chroma), (green3 = secondComponent))
    : huePrime >= 1 && huePrime < 2
    ? ((red3 = secondComponent), (green3 = chroma))
    : huePrime >= 2 && huePrime < 3
    ? ((green3 = chroma), (blue3 = secondComponent))
    : huePrime >= 3 && huePrime < 4
    ? ((green3 = secondComponent), (blue3 = chroma))
    : huePrime >= 4 && huePrime < 5
    ? ((red3 = secondComponent), (blue3 = chroma))
    : huePrime >= 5 && huePrime < 6 && ((red3 = chroma), (blue3 = secondComponent))
  var lightnessModification = lightness - chroma / 2,
    finalRed = red3 + lightnessModification,
    finalGreen = green3 + lightnessModification,
    finalBlue = blue3 + lightnessModification
  return convert(finalRed, finalGreen, finalBlue)
}
var namedColorMap = {
  aliceblue: 'f0f8ff',
  antiquewhite: 'faebd7',
  aqua: '00ffff',
  aquamarine: '7fffd4',
  azure: 'f0ffff',
  beige: 'f5f5dc',
  bisque: 'ffe4c4',
  black: '000',
  blanchedalmond: 'ffebcd',
  blue: '0000ff',
  blueviolet: '8a2be2',
  brown: 'a52a2a',
  burlywood: 'deb887',
  cadetblue: '5f9ea0',
  chartreuse: '7fff00',
  chocolate: 'd2691e',
  coral: 'ff7f50',
  cornflowerblue: '6495ed',
  cornsilk: 'fff8dc',
  crimson: 'dc143c',
  cyan: '00ffff',
  darkblue: '00008b',
  darkcyan: '008b8b',
  darkgoldenrod: 'b8860b',
  darkgray: 'a9a9a9',
  darkgreen: '006400',
  darkgrey: 'a9a9a9',
  darkkhaki: 'bdb76b',
  darkmagenta: '8b008b',
  darkolivegreen: '556b2f',
  darkorange: 'ff8c00',
  darkorchid: '9932cc',
  darkred: '8b0000',
  darksalmon: 'e9967a',
  darkseagreen: '8fbc8f',
  darkslateblue: '483d8b',
  darkslategray: '2f4f4f',
  darkslategrey: '2f4f4f',
  darkturquoise: '00ced1',
  darkviolet: '9400d3',
  deeppink: 'ff1493',
  deepskyblue: '00bfff',
  dimgray: '696969',
  dimgrey: '696969',
  dodgerblue: '1e90ff',
  firebrick: 'b22222',
  floralwhite: 'fffaf0',
  forestgreen: '228b22',
  fuchsia: 'ff00ff',
  gainsboro: 'dcdcdc',
  ghostwhite: 'f8f8ff',
  gold: 'ffd700',
  goldenrod: 'daa520',
  gray: '808080',
  green: '008000',
  greenyellow: 'adff2f',
  grey: '808080',
  honeydew: 'f0fff0',
  hotpink: 'ff69b4',
  indianred: 'cd5c5c',
  indigo: '4b0082',
  ivory: 'fffff0',
  khaki: 'f0e68c',
  lavender: 'e6e6fa',
  lavenderblush: 'fff0f5',
  lawngreen: '7cfc00',
  lemonchiffon: 'fffacd',
  lightblue: 'add8e6',
  lightcoral: 'f08080',
  lightcyan: 'e0ffff',
  lightgoldenrodyellow: 'fafad2',
  lightgray: 'd3d3d3',
  lightgreen: '90ee90',
  lightgrey: 'd3d3d3',
  lightpink: 'ffb6c1',
  lightsalmon: 'ffa07a',
  lightseagreen: '20b2aa',
  lightskyblue: '87cefa',
  lightslategray: '789',
  lightslategrey: '789',
  lightsteelblue: 'b0c4de',
  lightyellow: 'ffffe0',
  lime: '0f0',
  limegreen: '32cd32',
  linen: 'faf0e6',
  magenta: 'f0f',
  maroon: '800000',
  mediumaquamarine: '66cdaa',
  mediumblue: '0000cd',
  mediumorchid: 'ba55d3',
  mediumpurple: '9370db',
  mediumseagreen: '3cb371',
  mediumslateblue: '7b68ee',
  mediumspringgreen: '00fa9a',
  mediumturquoise: '48d1cc',
  mediumvioletred: 'c71585',
  midnightblue: '191970',
  mintcream: 'f5fffa',
  mistyrose: 'ffe4e1',
  moccasin: 'ffe4b5',
  navajowhite: 'ffdead',
  navy: '000080',
  oldlace: 'fdf5e6',
  olive: '808000',
  olivedrab: '6b8e23',
  orange: 'ffa500',
  orangered: 'ff4500',
  orchid: 'da70d6',
  palegoldenrod: 'eee8aa',
  palegreen: '98fb98',
  paleturquoise: 'afeeee',
  palevioletred: 'db7093',
  papayawhip: 'ffefd5',
  peachpuff: 'ffdab9',
  peru: 'cd853f',
  pink: 'ffc0cb',
  plum: 'dda0dd',
  powderblue: 'b0e0e6',
  purple: '800080',
  rebeccapurple: '639',
  red: 'f00',
  rosybrown: 'bc8f8f',
  royalblue: '4169e1',
  saddlebrown: '8b4513',
  salmon: 'fa8072',
  sandybrown: 'f4a460',
  seagreen: '2e8b57',
  seashell: 'fff5ee',
  sienna: 'a0522d',
  silver: 'c0c0c0',
  skyblue: '87ceeb',
  slateblue: '6a5acd',
  slategray: '708090',
  slategrey: '708090',
  snow: 'fffafa',
  springgreen: '00ff7f',
  steelblue: '4682b4',
  tan: 'd2b48c',
  teal: '008080',
  thistle: 'd8bfd8',
  tomato: 'ff6347',
  turquoise: '40e0d0',
  violet: 'ee82ee',
  wheat: 'f5deb3',
  white: 'fff',
  whitesmoke: 'f5f5f5',
  yellow: 'ff0',
  yellowgreen: '9acd32',
}
function nameToHex(color) {
  if (typeof color != 'string') return color
  var normalizedColorName = color.toLowerCase()
  return namedColorMap[normalizedColorName] ? '#' + namedColorMap[normalizedColorName] : color
}
var hexRegex = /^#[a-fA-F0-9]{6}$/,
  hexRgbaRegex = /^#[a-fA-F0-9]{8}$/,
  reducedHexRegex = /^#[a-fA-F0-9]{3}$/,
  reducedRgbaHexRegex = /^#[a-fA-F0-9]{4}$/,
  rgbRegex = /^rgb\(\s*(\d{1,3})\s*(?:,)?\s*(\d{1,3})\s*(?:,)?\s*(\d{1,3})\s*\)$/i,
  rgbaRegex =
    /^rgb(?:a)?\(\s*(\d{1,3})\s*(?:,)?\s*(\d{1,3})\s*(?:,)?\s*(\d{1,3})\s*(?:,|\/)\s*([-+]?\d*[.]?\d+[%]?)\s*\)$/i,
  hslRegex =
    /^hsl\(\s*(\d{0,3}[.]?[0-9]+(?:deg)?)\s*(?:,)?\s*(\d{1,3}[.]?[0-9]?)%\s*(?:,)?\s*(\d{1,3}[.]?[0-9]?)%\s*\)$/i,
  hslaRegex =
    /^hsl(?:a)?\(\s*(\d{0,3}[.]?[0-9]+(?:deg)?)\s*(?:,)?\s*(\d{1,3}[.]?[0-9]?)%\s*(?:,)?\s*(\d{1,3}[.]?[0-9]?)%\s*(?:,|\/)\s*([-+]?\d*[.]?\d+[%]?)\s*\)$/i
function parseToRgb(color) {
  if (typeof color != 'string') throw new PolishedError(3)
  var normalizedColor = nameToHex(color)
  if (normalizedColor.match(hexRegex))
    return {
      red: parseInt('' + normalizedColor[1] + normalizedColor[2], 16),
      green: parseInt('' + normalizedColor[3] + normalizedColor[4], 16),
      blue: parseInt('' + normalizedColor[5] + normalizedColor[6], 16),
    }
  if (normalizedColor.match(hexRgbaRegex)) {
    var alpha = parseFloat(
      (parseInt('' + normalizedColor[7] + normalizedColor[8], 16) / 255).toFixed(2)
    )
    return {
      red: parseInt('' + normalizedColor[1] + normalizedColor[2], 16),
      green: parseInt('' + normalizedColor[3] + normalizedColor[4], 16),
      blue: parseInt('' + normalizedColor[5] + normalizedColor[6], 16),
      alpha,
    }
  }
  if (normalizedColor.match(reducedHexRegex))
    return {
      red: parseInt('' + normalizedColor[1] + normalizedColor[1], 16),
      green: parseInt('' + normalizedColor[2] + normalizedColor[2], 16),
      blue: parseInt('' + normalizedColor[3] + normalizedColor[3], 16),
    }
  if (normalizedColor.match(reducedRgbaHexRegex)) {
    var _alpha = parseFloat(
      (parseInt('' + normalizedColor[4] + normalizedColor[4], 16) / 255).toFixed(2)
    )
    return {
      red: parseInt('' + normalizedColor[1] + normalizedColor[1], 16),
      green: parseInt('' + normalizedColor[2] + normalizedColor[2], 16),
      blue: parseInt('' + normalizedColor[3] + normalizedColor[3], 16),
      alpha: _alpha,
    }
  }
  var rgbMatched = rgbRegex.exec(normalizedColor)
  if (rgbMatched)
    return {
      red: parseInt('' + rgbMatched[1], 10),
      green: parseInt('' + rgbMatched[2], 10),
      blue: parseInt('' + rgbMatched[3], 10),
    }
  var rgbaMatched = rgbaRegex.exec(normalizedColor.substring(0, 50))
  if (rgbaMatched)
    return {
      red: parseInt('' + rgbaMatched[1], 10),
      green: parseInt('' + rgbaMatched[2], 10),
      blue: parseInt('' + rgbaMatched[3], 10),
      alpha:
        parseFloat('' + rgbaMatched[4]) > 1
          ? parseFloat('' + rgbaMatched[4]) / 100
          : parseFloat('' + rgbaMatched[4]),
    }
  var hslMatched = hslRegex.exec(normalizedColor)
  if (hslMatched) {
    var hue = parseInt('' + hslMatched[1], 10),
      saturation = parseInt('' + hslMatched[2], 10) / 100,
      lightness = parseInt('' + hslMatched[3], 10) / 100,
      rgbColorString = 'rgb(' + hslToRgb(hue, saturation, lightness) + ')',
      hslRgbMatched = rgbRegex.exec(rgbColorString)
    if (!hslRgbMatched) throw new PolishedError(4, normalizedColor, rgbColorString)
    return {
      red: parseInt('' + hslRgbMatched[1], 10),
      green: parseInt('' + hslRgbMatched[2], 10),
      blue: parseInt('' + hslRgbMatched[3], 10),
    }
  }
  var hslaMatched = hslaRegex.exec(normalizedColor.substring(0, 50))
  if (hslaMatched) {
    var _hue = parseInt('' + hslaMatched[1], 10),
      _saturation = parseInt('' + hslaMatched[2], 10) / 100,
      _lightness = parseInt('' + hslaMatched[3], 10) / 100,
      _rgbColorString = 'rgb(' + hslToRgb(_hue, _saturation, _lightness) + ')',
      _hslRgbMatched = rgbRegex.exec(_rgbColorString)
    if (!_hslRgbMatched) throw new PolishedError(4, normalizedColor, _rgbColorString)
    return {
      red: parseInt('' + _hslRgbMatched[1], 10),
      green: parseInt('' + _hslRgbMatched[2], 10),
      blue: parseInt('' + _hslRgbMatched[3], 10),
      alpha:
        parseFloat('' + hslaMatched[4]) > 1
          ? parseFloat('' + hslaMatched[4]) / 100
          : parseFloat('' + hslaMatched[4]),
    }
  }
  throw new PolishedError(5)
}
var reduceHexValue = function (value) {
    return value.length === 7 &&
      value[1] === value[2] &&
      value[3] === value[4] &&
      value[5] === value[6]
      ? '#' + value[1] + value[3] + value[5]
      : value
  },
  reduceHexValue$1 = reduceHexValue
function numberToHex(value) {
  var hex = value.toString(16)
  return hex.length === 1 ? '0' + hex : hex
}
function rgb(value, green3, blue3) {
  if (typeof value == 'number' && typeof green3 == 'number' && typeof blue3 == 'number')
    return reduceHexValue$1('#' + numberToHex(value) + numberToHex(green3) + numberToHex(blue3))
  if (typeof value == 'object' && green3 === void 0 && blue3 === void 0)
    return reduceHexValue$1(
      '#' + numberToHex(value.red) + numberToHex(value.green) + numberToHex(value.blue)
    )
  throw new PolishedError(6)
}
function rgba(firstValue, secondValue, thirdValue, fourthValue) {
  if (typeof firstValue == 'string' && typeof secondValue == 'number') {
    var rgbValue = parseToRgb(firstValue)
    return (
      'rgba(' + rgbValue.red + ',' + rgbValue.green + ',' + rgbValue.blue + ',' + secondValue + ')'
    )
  } else {
    if (
      typeof firstValue == 'number' &&
      typeof secondValue == 'number' &&
      typeof thirdValue == 'number' &&
      typeof fourthValue == 'number'
    )
      return fourthValue >= 1
        ? rgb(firstValue, secondValue, thirdValue)
        : 'rgba(' + firstValue + ',' + secondValue + ',' + thirdValue + ',' + fourthValue + ')'
    if (
      typeof firstValue == 'object' &&
      secondValue === void 0 &&
      thirdValue === void 0 &&
      fourthValue === void 0
    )
      return firstValue.alpha >= 1
        ? rgb(firstValue.red, firstValue.green, firstValue.blue)
        : 'rgba(' +
            firstValue.red +
            ',' +
            firstValue.green +
            ',' +
            firstValue.blue +
            ',' +
            firstValue.alpha +
            ')'
  }
  throw new PolishedError(7)
}
function curried(f, length, acc) {
  return function () {
    var combined = acc.concat(Array.prototype.slice.call(arguments))
    return combined.length >= length ? f.apply(this, combined) : curried(f, length, combined)
  }
}
function curry(f) {
  return curried(f, f.length, [])
}
function mix(weight, color, otherColor) {
  if (color === 'transparent') return otherColor
  if (otherColor === 'transparent') return color
  if (weight === 0) return otherColor
  var parsedColor1 = parseToRgb(color),
    color1 = _extends({}, parsedColor1, {
      alpha: typeof parsedColor1.alpha == 'number' ? parsedColor1.alpha : 1,
    }),
    parsedColor2 = parseToRgb(otherColor),
    color2 = _extends({}, parsedColor2, {
      alpha: typeof parsedColor2.alpha == 'number' ? parsedColor2.alpha : 1,
    }),
    alphaDelta = color1.alpha - color2.alpha,
    x = parseFloat(weight) * 2 - 1,
    y = x * alphaDelta === -1 ? x : x + alphaDelta,
    z = 1 + x * alphaDelta,
    weight1 = (y / z + 1) / 2,
    weight2 = 1 - weight1,
    mixedColor = {
      red: Math.floor(color1.red * weight1 + color2.red * weight2),
      green: Math.floor(color1.green * weight1 + color2.green * weight2),
      blue: Math.floor(color1.blue * weight1 + color2.blue * weight2),
      alpha: color1.alpha * parseFloat(weight) + color2.alpha * (1 - parseFloat(weight)),
    }
  return rgba(mixedColor)
}
var curriedMix = /* @__PURE__ */ curry(mix),
  mix$1 = curriedMix

// utils/createTonesFromHues.ts
function createTonesFromHues(hues3) {
  return {
    default: createTintsFromHue(hues3.default, 'Default'),
    primary: createTintsFromHue(hues3.primary, 'Primary'),
    transparent: createTintsFromHue(hues3.transparent, 'Transparent'),
    positive: createTintsFromHue(hues3.positive, 'Positive'),
    caution: createTintsFromHue(hues3.caution, 'Caution'),
    critical: createTintsFromHue(hues3.critical, 'Critical'),
  }
}
function createTintsFromHue(hue, title) {
  let initial = {}
  return COLOR_TINTS.reduce(
    (acc, tint) => (
      (acc[tint] = {
        title: ''.concat(title, ' ').concat(tint),
        hex: getColorHex(hue, tint),
      }),
      acc
    ),
    initial
  )
}
function getColorHex(hue, tint) {
  let tintNum = Number(tint),
    midPoint = hue.midPoint,
    darkSize = 1e3 - midPoint,
    lightPosition = tintNum / midPoint,
    darkPosition = (tintNum - midPoint) / darkSize
  return tintNum === midPoint
    ? hue.mid.toLowerCase()
    : tintNum < midPoint
    ? mix$1(lightPosition, hue.mid, hue.lightest)
    : mix$1(darkPosition, hue.darkest, hue.mid)
}

// utils/themeFromHues.ts
function getTint(key) {
  switch (key) {
    case 'blue':
      return blue2
    case 'cyan':
      return cyan2
    case 'gray':
      return gray2
    case 'green':
      return green2
    case 'magenta':
      return magenta2
    case 'orange':
      return orange2
    case 'purple':
      return purple2
    case 'red':
      return red2
    case 'yellow':
      return yellow2
    default:
      throw new Error('Unknown tint: '.concat(key))
  }
}
function themeFromHues({
  hues: partialHues,
  studioTheme: studioTheme2,
  multiply: _multiply,
  screen: _screen,
  parseColor: parseColor2,
  rgbToHex: rgbToHex2,
  rgba: rgba3,
  createColorTheme: createColorTheme2,
}) {
  function multiply2(bg, fg) {
    let b = parseColor2(bg),
      s = parseColor2(fg)
    return rgbToHex2(_multiply(b, s))
  }
  function screen2(bg, fg) {
    let b = parseColor2(bg),
      s = parseColor2(fg)
    return rgbToHex2(_screen(b, s))
  }
  let hues3 = applyHues(partialHues),
    black3 = {title: 'Black', hex: hues3.default.darkest},
    white3 = {title: 'white', hex: hues3.default.lightest},
    tones2 = createTonesFromHues(hues3),
    focusRingHue = tones2.primary,
    accentHue = tones2.critical,
    linkHue = tones2.primary,
    color = createColorTheme2({
      base: ({dark, name}) => {
        if (name === 'default') {
          let skeletonFrom2 = dark ? tones2.transparent[900].hex : tones2.transparent[100].hex
          return {
            // @TODO: consider making this overridable
            fg: dark ? white3.hex : black3.hex,
            // @TODO: consider making this overridable
            bg: dark ? black3.hex : white3.hex,
            // @TODO: consider making this overridable
            border: tones2.transparent[dark ? 800 : 200].hex,
            focusRing: focusRingHue[500].hex,
            shadow: {
              outline: rgba3(tones2.transparent[500].hex, 0.4),
              umbra: rgba3(dark ? black3.hex : tones2.transparent[500].hex, 0.2),
              penumbra: rgba3(dark ? black3.hex : tones2.transparent[500].hex, 0.14),
              ambient: rgba3(dark ? black3.hex : tones2.transparent[500].hex, 0.12),
            },
            skeleton: {
              from: skeletonFrom2,
              to: rgba3(skeletonFrom2, 0.5),
            },
          }
        }
        if (name === 'transparent') {
          let tints2 = tones2.default,
            skeletonFrom2 = tints2[dark ? 800 : 200].hex
          return {
            fg: tints2[dark ? 100 : 900].hex,
            bg: tints2[dark ? 950 : 50].hex,
            border: tints2[dark ? 800 : 300].hex,
            focusRing: focusRingHue[500].hex,
            shadow: {
              outline: rgba3(tints2[500].hex, dark ? 0.2 : 0.4),
              umbra: rgba3(dark ? black3.hex : tints2[500].hex, 0.2),
              penumbra: rgba3(dark ? black3.hex : tints2[500].hex, 0.14),
              ambient: rgba3(dark ? black3.hex : tints2[500].hex, 0.12),
            },
            skeleton: {
              from: skeletonFrom2,
              to: rgba3(skeletonFrom2, 0.5),
            },
          }
        }
        let tints = tones2[name] || tones2.default,
          skeletonFrom = tints[dark ? 800 : 200].hex
        return {
          fg: tints[dark ? 100 : 900].hex,
          bg: tints[dark ? 950 : 50].hex,
          border: tints[dark ? 800 : 200].hex,
          focusRing: tints[500].hex,
          shadow: {
            outline: rgba3(tints[500].hex, dark ? 0.2 : 0.4),
            umbra: rgba3(dark ? black3.hex : tints[500].hex, 0.2),
            penumbra: rgba3(dark ? black3.hex : tints[500].hex, 0.14),
            ambient: rgba3(dark ? black3.hex : tints[500].hex, 0.12),
          },
          skeleton: {
            from: skeletonFrom,
            to: rgba3(skeletonFrom, 0.5),
          },
        }
      },
      solid: ({base, dark, name, state, tone}) => {
        let mix2 = dark ? screen2 : multiply2,
          mix22 = dark ? multiply2 : screen2,
          defaultTints = tones2[name] || tones2.default,
          isNeutral = NEUTRAL_TONES.includes(name) && NEUTRAL_TONES.includes(tone),
          tints = tones2[tone === 'default' ? name : tone] || defaultTints
        if (state === 'disabled') {
          tints = defaultTints
          let bg2 = mix2(base.bg, tints[dark ? 800 : 200].hex),
            skeletonFrom2 = mix22(bg2, tints[dark ? 200 : 800].hex)
          return {
            bg: bg2,
            bg2: mix22(bg2, tints[dark ? 50 : 950].hex),
            border: mix2(base.bg, tints[dark ? 800 : 200].hex),
            fg: mix2(base.bg, dark ? black3.hex : white3.hex),
            muted: {
              fg: mix2(base.bg, tints[dark ? 950 : 50].hex),
            },
            accent: {
              fg: mix2(base.bg, tints[dark ? 950 : 50].hex),
            },
            link: {
              fg: mix2(base.bg, tints[dark ? 950 : 50].hex),
            },
            code: {
              bg: bg2,
              fg: mix2(base.bg, tints[dark ? 950 : 50].hex),
            },
            skeleton: {
              from: skeletonFrom2,
              to: rgba3(skeletonFrom2, 0.5),
            },
          }
        }
        if (state === 'hovered') {
          let bg2 = mix2(base.bg, tints[dark ? 300 : 600].hex),
            skeletonFrom2 = mix22(bg2, tints[dark ? 200 : 800].hex)
          return {
            bg: bg2,
            bg2: mix22(bg2, tints[dark ? 50 : 950].hex),
            border: mix2(base.bg, tints[dark ? 300 : 600].hex),
            fg: mix2(base.bg, dark ? black3.hex : white3.hex),
            muted: {
              fg: mix2(base.bg, tints[dark ? 800 : 200].hex),
            },
            accent: {
              fg: mix22(bg2, accentHue[dark ? 800 : 200].hex),
            },
            link: {
              fg: mix22(bg2, linkHue[dark ? 800 : 200].hex),
            },
            code: {
              bg: mix2(bg2, tints[dark ? 950 : 50].hex),
              fg: mix2(base.bg, tints[dark ? 800 : 200].hex),
            },
            skeleton: {
              from: skeletonFrom2,
              to: rgba3(skeletonFrom2, 0.5),
            },
          }
        }
        if (state === 'pressed') {
          let bg2 = mix2(base.bg, tints[dark ? 200 : 800].hex),
            skeletonFrom2 = mix22(bg2, tints[dark ? 200 : 800].hex)
          return {
            bg: mix2(base.bg, tints[dark ? 200 : 800].hex),
            bg2: mix22(bg2, tints[dark ? 50 : 950].hex),
            border: mix2(base.bg, tints[dark ? 200 : 800].hex),
            fg: mix2(base.bg, dark ? black3.hex : white3.hex),
            muted: {
              fg: mix2(base.bg, tints[dark ? 800 : 200].hex),
            },
            accent: {
              fg: mix22(bg2, accentHue[dark ? 800 : 200].hex),
            },
            link: {
              fg: mix22(bg2, linkHue[dark ? 800 : 200].hex),
            },
            code: {
              bg: mix2(bg2, tints[dark ? 950 : 50].hex),
              fg: mix2(base.bg, tints[dark ? 800 : 200].hex),
            },
            skeleton: {
              from: skeletonFrom2,
              to: rgba3(skeletonFrom2, 0.5),
            },
          }
        }
        if (state === 'selected') {
          isNeutral && (tints = tones2.primary)
          let bg2 = mix2(base.bg, tints[dark ? 200 : 800].hex),
            skeletonFrom2 = mix22(bg2, tints[dark ? 200 : 800].hex)
          return {
            bg: bg2,
            bg2: mix22(bg2, tints[dark ? 50 : 950].hex),
            border: mix2(base.bg, tints[dark ? 200 : 800].hex),
            fg: mix2(base.bg, dark ? black3.hex : white3.hex),
            muted: {
              fg: mix2(base.bg, tints[dark ? 800 : 200].hex),
            },
            accent: {
              fg: mix22(bg2, accentHue[dark ? 800 : 200].hex),
            },
            link: {
              fg: mix22(bg2, linkHue[dark ? 800 : 200].hex),
            },
            code: {
              bg: mix2(bg2, tints[dark ? 950 : 50].hex),
              fg: mix2(base.bg, tints[dark ? 800 : 200].hex),
            },
            skeleton: {
              from: skeletonFrom2,
              to: rgba3(skeletonFrom2, 0.5),
            },
          }
        }
        let bg = mix2(base.bg, tints[dark ? 400 : 500].hex),
          skeletonFrom = mix22(bg, tints[dark ? 200 : 800].hex)
        return {
          bg,
          bg2: mix22(bg, tints[dark ? 50 : 950].hex),
          border: mix2(base.bg, tints[dark ? 400 : 500].hex),
          fg: mix2(base.bg, dark ? black3.hex : white3.hex),
          muted: {
            fg: mix2(base.bg, tints[dark ? 900 : 100].hex),
          },
          accent: {
            fg: mix22(bg, accentHue[dark ? 900 : 100].hex),
          },
          link: {
            fg: mix22(bg, linkHue[dark ? 900 : 100].hex),
          },
          code: {
            bg: mix2(bg, tints[dark ? 950 : 50].hex),
            fg: mix2(base.bg, tints[dark ? 900 : 100].hex),
          },
          skeleton: {
            from: skeletonFrom,
            to: rgba3(skeletonFrom, 0.5),
          },
        }
      },
      muted: ({base, dark, name, state, tone}) => {
        let mix2 = dark ? screen2 : multiply2,
          defaultTints = tones2[name] || tones2.default,
          isNeutral = NEUTRAL_TONES.includes(name) && NEUTRAL_TONES.includes(tone),
          tints = tones2[tone === 'default' ? name : tone] || defaultTints
        if (state === 'disabled') {
          tints = defaultTints
          let bg2 = base.bg,
            skeletonFrom2 = mix2(bg2, tints[dark ? 900 : 100].hex)
          return {
            bg: bg2,
            bg2: mix2(bg2, tints[dark ? 950 : 50].hex),
            border: mix2(bg2, tints[dark ? 950 : 50].hex),
            fg: mix2(bg2, tints[dark ? 800 : 200].hex),
            muted: {
              fg: mix2(bg2, tints[dark ? 900 : 100].hex),
            },
            accent: {
              fg: mix2(bg2, tints[dark ? 900 : 100].hex),
            },
            link: {
              fg: mix2(bg2, tints[dark ? 900 : 100].hex),
            },
            code: {
              bg: bg2,
              fg: mix2(bg2, tints[dark ? 900 : 100].hex),
            },
            skeleton: {
              from: rgba3(skeletonFrom2, 0.5),
              to: rgba3(skeletonFrom2, 0.25),
            },
          }
        }
        if (state === 'hovered') {
          isNeutral && (tints = tones2.primary)
          let bg2 = mix2(base.bg, tints[dark ? 950 : 50].hex),
            skeletonFrom2 = mix2(bg2, tints[dark ? 900 : 100].hex)
          return {
            bg: bg2,
            bg2: mix2(bg2, tints[dark ? 950 : 50].hex),
            border: mix2(bg2, tints[dark ? 900 : 100].hex),
            fg: mix2(base.bg, tints[dark ? 200 : 800].hex),
            muted: {
              fg: mix2(base.bg, tints[dark ? 400 : 600].hex),
            },
            accent: {
              fg: mix2(base.bg, linkHue[dark ? 400 : 500].hex),
            },
            link: {
              fg: mix2(base.bg, linkHue[dark ? 400 : 600].hex),
            },
            code: {
              bg: mix2(bg2, tints[dark ? 950 : 50].hex),
              fg: mix2(base.bg, tints[dark ? 400 : 600].hex),
            },
            skeleton: {
              from: skeletonFrom2,
              to: rgba3(skeletonFrom2, 0.5),
            },
          }
        }
        if (state === 'pressed') {
          isNeutral && (tints = tones2.primary)
          let bg2 = mix2(base.bg, tints[dark ? 900 : 100].hex),
            skeletonFrom2 = mix2(bg2, tints[dark ? 900 : 100].hex)
          return {
            bg: bg2,
            bg2: mix2(bg2, tints[dark ? 950 : 50].hex),
            border: mix2(bg2, tints[dark ? 900 : 100].hex),
            fg: mix2(base.bg, tints[dark ? 200 : 800].hex),
            muted: {
              fg: mix2(base.bg, tints[dark ? 400 : 600].hex),
            },
            accent: {
              fg: mix2(bg2, accentHue[dark ? 400 : 500].hex),
            },
            link: {
              fg: mix2(bg2, linkHue[dark ? 400 : 600].hex),
            },
            code: {
              bg: mix2(bg2, tints[dark ? 950 : 50].hex),
              fg: mix2(base.bg, tints[dark ? 400 : 600].hex),
            },
            skeleton: {
              from: skeletonFrom2,
              to: rgba3(skeletonFrom2, 0.5),
            },
          }
        }
        if (state === 'selected') {
          isNeutral && (tints = tones2.primary)
          let bg2 = mix2(base.bg, tints[dark ? 900 : 100].hex),
            skeletonFrom2 = mix2(bg2, tints[dark ? 900 : 100].hex)
          return {
            bg: bg2,
            bg2: mix2(bg2, tints[dark ? 950 : 50].hex),
            border: mix2(bg2, tints[dark ? 900 : 100].hex),
            fg: mix2(base.bg, tints[dark ? 200 : 800].hex),
            muted: {
              fg: mix2(base.bg, tints[dark ? 400 : 600].hex),
            },
            accent: {
              fg: mix2(bg2, accentHue[dark ? 400 : 500].hex),
            },
            link: {
              fg: mix2(bg2, linkHue[dark ? 400 : 600].hex),
            },
            code: {
              bg: mix2(bg2, tints[dark ? 950 : 50].hex),
              fg: mix2(base.bg, tints[dark ? 400 : 600].hex),
            },
            skeleton: {
              from: skeletonFrom2,
              to: rgba3(skeletonFrom2, 0.5),
            },
          }
        }
        let bg = base.bg,
          skeletonFrom = mix2(bg, tints[dark ? 900 : 100].hex)
        return {
          bg,
          bg2: mix2(bg, tints[dark ? 950 : 50].hex),
          border: mix2(bg, tints[dark ? 900 : 100].hex),
          fg: mix2(base.bg, tints[dark ? 300 : 700].hex),
          muted: {
            fg: mix2(base.bg, tints[dark ? 400 : 600].hex),
          },
          accent: {
            fg: mix2(base.bg, accentHue[dark ? 400 : 500].hex),
          },
          link: {
            fg: mix2(base.bg, linkHue[dark ? 400 : 600].hex),
          },
          code: {
            bg: mix2(base.bg, tints[dark ? 950 : 50].hex),
            fg: mix2(base.bg, tints[dark ? 400 : 600].hex),
          },
          skeleton: {
            from: skeletonFrom,
            to: rgba3(skeletonFrom, 0.5),
          },
        }
      },
      button: ({base, mode, muted, solid}) =>
        mode === 'bleed'
          ? {
              enabled: {
                ...muted.enabled,
                border: muted.enabled.bg,
              },
              hovered: {
                ...muted.hovered,
                border: muted.hovered.bg,
              },
              pressed: {
                ...muted.pressed,
                border: muted.pressed.bg,
              },
              selected: {
                ...muted.selected,
                border: muted.selected.bg,
              },
              disabled: {
                ...muted.disabled,
                border: muted.disabled.bg,
              },
            }
          : mode === 'ghost'
          ? {
              ...solid,
              enabled: {
                ...muted.enabled,
                border: base.border,
              },
              disabled: muted.disabled,
            }
          : solid,
      card: ({base, dark, muted, name, solid, state}) => {
        if (state === 'hovered') return muted[name].hovered
        if (state === 'disabled') return muted[name].disabled
        let isNeutral = NEUTRAL_TONES.includes(name),
          tints = tones2[name] || tones2.default,
          mix2 = dark ? screen2 : multiply2
        if (state === 'pressed') return isNeutral ? muted.primary.pressed : muted[name].pressed
        if (state === 'selected') return isNeutral ? solid.primary.enabled : solid[name].enabled
        let bg = base.bg,
          skeletonFrom = mix2(base.bg, tints[dark ? 900 : 100].hex)
        return {
          bg,
          bg2: mix2(bg, tints[dark ? 950 : 50].hex),
          fg: base.fg,
          border: base.border,
          muted: {
            fg: mix2(base.bg, tints[dark ? 400 : 600].hex),
          },
          accent: {
            fg: mix2(base.bg, red2[dark ? 400 : 500].hex),
          },
          link: {
            fg: mix2(base.bg, blue2[dark ? 400 : 600].hex),
          },
          code: {
            bg: mix2(base.bg, tints[dark ? 950 : 50].hex),
            fg: tints[dark ? 400 : 600].hex,
          },
          skeleton: {
            from: skeletonFrom,
            to: rgba3(skeletonFrom, 0.5),
          },
        }
      },
      input: ({base, dark, mode, state}) => {
        let mix2 = dark ? screen2 : multiply2
        if (mode === 'invalid') {
          let tints = tones2.critical
          return {
            bg: mix2(base.bg, tints[dark ? 950 : 50].hex),
            fg: mix2(base.bg, tints[dark ? 400 : 600].hex),
            border: mix2(base.bg, tints[dark ? 800 : 200].hex),
            placeholder: mix2(base.bg, tints[dark ? 600 : 400].hex),
          }
        }
        return state === 'hovered'
          ? {
              bg: base.bg,
              fg: base.fg,
              border: mix2(base.bg, gray2[dark ? 700 : 300].hex),
              placeholder: mix2(base.bg, gray2[dark ? 600 : 400].hex),
            }
          : state === 'disabled'
          ? {
              bg: mix2(base.bg, gray2[dark ? 950 : 50].hex),
              fg: mix2(base.bg, gray2[dark ? 700 : 300].hex),
              border: mix2(base.bg, gray2[dark ? 900 : 100].hex),
              placeholder: mix2(base.bg, gray2[dark ? 800 : 200].hex),
            }
          : state === 'readOnly'
          ? {
              bg: mix2(base.bg, gray2[dark ? 950 : 50].hex),
              fg: mix2(base.bg, gray2[dark ? 200 : 800].hex),
              border: mix2(base.bg, gray2[dark ? 800 : 200].hex),
              placeholder: mix2(base.bg, gray2[dark ? 600 : 400].hex),
            }
          : {
              bg: base.bg,
              fg: base.fg,
              border: base.border,
              placeholder: mix2(base.bg, gray2[dark ? 600 : 400].hex),
            }
      },
      selectable: ({base, muted, tone, solid, state}) =>
        state === 'enabled'
          ? {
              ...muted[tone].enabled,
              bg: base.bg,
            }
          : state === 'pressed'
          ? tone === 'default'
            ? muted.primary.pressed
            : muted[tone].pressed
          : state === 'selected'
          ? tone === 'default'
            ? solid.primary.enabled
            : solid[tone].enabled
          : state === 'disabled'
          ? {
              ...muted[tone].disabled,
              bg: base.bg,
            }
          : muted[tone][state],
      spot: ({base, dark, key}) =>
        (dark ? screen2 : multiply2)(base.bg, getTint(key)[dark ? 400 : 500].hex),
      syntax: ({base, dark}) => {
        let mix2 = dark ? screen2 : multiply2,
          mainShade = dark ? 400 : 600,
          secondaryShade = dark ? 600 : 400
        return {
          atrule: mix2(base.bg, purple2[mainShade].hex),
          attrName: mix2(base.bg, green2[mainShade].hex),
          attrValue: mix2(base.bg, yellow2[mainShade].hex),
          attribute: mix2(base.bg, yellow2[mainShade].hex),
          boolean: mix2(base.bg, purple2[mainShade].hex),
          builtin: mix2(base.bg, purple2[mainShade].hex),
          cdata: mix2(base.bg, yellow2[mainShade].hex),
          char: mix2(base.bg, yellow2[mainShade].hex),
          class: mix2(base.bg, orange2[mainShade].hex),
          className: mix2(base.bg, cyan2[mainShade].hex),
          comment: mix2(base.bg, gray2[secondaryShade].hex),
          constant: mix2(base.bg, purple2[mainShade].hex),
          deleted: mix2(base.bg, red2[mainShade].hex),
          doctype: mix2(base.bg, gray2[secondaryShade].hex),
          entity: mix2(base.bg, red2[mainShade].hex),
          function: mix2(base.bg, green2[mainShade].hex),
          hexcode: mix2(base.bg, blue2[mainShade].hex),
          id: mix2(base.bg, purple2[mainShade].hex),
          important: mix2(base.bg, purple2[mainShade].hex),
          inserted: mix2(base.bg, yellow2[mainShade].hex),
          keyword: mix2(base.bg, magenta2[mainShade].hex),
          number: mix2(base.bg, purple2[mainShade].hex),
          operator: mix2(base.bg, magenta2[mainShade].hex),
          prolog: mix2(base.bg, gray2[secondaryShade].hex),
          property: mix2(base.bg, blue2[mainShade].hex),
          pseudoClass: mix2(base.bg, yellow2[mainShade].hex),
          pseudoElement: mix2(base.bg, yellow2[mainShade].hex),
          punctuation: mix2(base.bg, gray2[mainShade].hex),
          regex: mix2(base.bg, blue2[mainShade].hex),
          selector: mix2(base.bg, red2[mainShade].hex),
          string: mix2(base.bg, yellow2[mainShade].hex),
          symbol: mix2(base.bg, purple2[mainShade].hex),
          tag: mix2(base.bg, red2[mainShade].hex),
          unit: mix2(base.bg, orange2[mainShade].hex),
          url: mix2(base.bg, red2[mainShade].hex),
          variable: mix2(base.bg, red2[mainShade].hex),
        }
      },
    })
  return {...studioTheme2, color}
}

// ../../node_modules/.pnpm/@sanity+ui@1.7.4_react-dom@18.2.0_react-is@18.2.0_react@18.2.0_styled-components@5.3.11/node_modules/@sanity/ui/src/theme/lib/color-fns/blend/multiply.ts
function multiplyChannel(b, s) {
  return b * s
}
function multiply(b, s) {
  return {
    r: Math.round(clamp(multiplyChannel(b.r / 255, s.r / 255) * 255)),
    g: Math.round(clamp(multiplyChannel(b.g / 255, s.g / 255) * 255)),
    b: Math.round(clamp(multiplyChannel(b.b / 255, s.b / 255) * 255)),
  }
}
function clamp(num) {
  return Math.max(Math.min(num, 255), 0)
}

// ../../node_modules/.pnpm/@sanity+ui@1.7.4_react-dom@18.2.0_react-is@18.2.0_react@18.2.0_styled-components@5.3.11/node_modules/@sanity/ui/src/theme/lib/color-fns/blend/screen.ts
function screenChannel(b, s) {
  return b + s - b * s
}
function screen(b, s) {
  return {
    r: Math.round(clamp2(screenChannel(b.r / 255, s.r / 255) * 255)),
    g: Math.round(clamp2(screenChannel(b.g / 255, s.g / 255) * 255)),
    b: Math.round(clamp2(screenChannel(b.b / 255, s.b / 255) * 255)),
  }
}
function clamp2(num) {
  return Math.max(Math.min(num, 255), 0)
}

// ../../node_modules/.pnpm/@sanity+ui@1.7.4_react-dom@18.2.0_react-is@18.2.0_react@18.2.0_styled-components@5.3.11/node_modules/@sanity/ui/src/theme/lib/color-fns/convert.ts
function hexToRgb(hex) {
  if (hex.length === 4) {
    let hexR = hex.slice(1, 2),
      hexG = hex.slice(2, 3),
      hexB = hex.slice(3, 4)
    return {
      r: parseInt(hexR + hexR, 16),
      g: parseInt(hexG + hexG, 16),
      b: parseInt(hexB + hexB, 16),
    }
  }
  return {
    r: parseInt(hex.slice(1, 3), 16),
    g: parseInt(hex.slice(3, 5), 16),
    b: parseInt(hex.slice(5, 7), 16),
  }
}
function rgbToHex({r, g, b}) {
  return '#' + ((1 << 24) + (r << 16) + (g << 8) + b).toString(16).slice(1)
}
function hslToRgb2(hsl) {
  let s = hsl.s / 100,
    l = hsl.l / 100,
    c = (1 - Math.abs(2 * l - 1)) * s,
    x = c * (1 - Math.abs(((hsl.h / 60) % 2) - 1)),
    m = l - c / 2,
    r = 0,
    g = 0,
    b = 0
  return (
    0 <= hsl.h && hsl.h < 60
      ? ((r = c), (g = x), (b = 0))
      : 60 <= hsl.h && hsl.h < 120
      ? ((r = x), (g = c), (b = 0))
      : 120 <= hsl.h && hsl.h < 180
      ? ((r = 0), (g = c), (b = x))
      : 180 <= hsl.h && hsl.h < 240
      ? ((r = 0), (g = x), (b = c))
      : 240 <= hsl.h && hsl.h < 300
      ? ((r = x), (g = 0), (b = c))
      : 300 <= hsl.h && hsl.h < 360 && ((r = c), (g = 0), (b = x)),
    {
      r: Math.round((r + m) * 255),
      g: Math.round((g + m) * 255),
      b: Math.round((b + m) * 255),
    }
  )
}

// ../../node_modules/.pnpm/@sanity+ui@1.7.4_react-dom@18.2.0_react-is@18.2.0_react@18.2.0_styled-components@5.3.11/node_modules/@sanity/ui/src/theme/lib/color-fns/parse.ts
var HEX_CHARS = '0123456789ABCDEFabcdef',
  HSL_RE = /hsl\(\s*(\d+)\s*,\s*((\d+(?:\.\d+)?)%)\s*,\s*((\d+(?:\.\d+)?)%)\s*\)/i
function isHexChars(str) {
  for (let c of str) if (HEX_CHARS.indexOf(c) === -1) return !1
  return !0
}
function isHex(str) {
  return str[0] !== '#' || !(str.length === 4 || str.length === 7) ? !1 : isHexChars(str.slice(1))
}
function parseHsl(str) {
  let res = HSL_RE.exec(str)
  if (!res) throw new Error('parseHsl: string is not a HSL color: "'.concat(str, '"'))
  return {h: parseInt(res[1]), s: parseFloat(res[3]), l: parseFloat(res[5])}
}
function parseColor(color) {
  if (!color) return {r: 0, g: 0, b: 0}
  if (typeof color != 'string') throw new Error('parseColor: expected a string')
  if (isHex(color)) return hexToRgb(color)
  if (color.startsWith('hsl(')) return hslToRgb2(parseHsl(color))
  throw new Error('parseColor: unexpected color format: "'.concat(color, '"'))
}

// ../../node_modules/.pnpm/@sanity+ui@1.7.4_react-dom@18.2.0_react-is@18.2.0_react@18.2.0_styled-components@5.3.11/node_modules/@sanity/ui/src/theme/lib/color-fns/helpers/rgba.ts
function rgba2(color, a) {
  let rgb2 = parseColor(color)
  return 'rgba('.concat(rgb2.r, ',').concat(rgb2.g, ',').concat(rgb2.b, ',').concat(a, ')')
}

// ../../node_modules/.pnpm/@sanity+ui@1.7.4_react-dom@18.2.0_react-is@18.2.0_react@18.2.0_styled-components@5.3.11/node_modules/@sanity/ui/src/theme/lib/theme/color/button/createButtonTones.ts
function createButtonTones(opts, base, dark, solid, muted, mode) {
  return {
    default: opts.button({
      base,
      dark,
      solid: solid.default,
      muted: muted.default,
      mode,
    }),
    primary: opts.button({
      base,
      dark,
      solid: solid.primary,
      muted: muted.primary,
      mode,
    }),
    positive: opts.button({
      base,
      dark,
      solid: solid.positive,
      muted: muted.positive,
      mode,
    }),
    caution: opts.button({
      base,
      dark,
      solid: solid.caution,
      muted: muted.caution,
      mode,
    }),
    critical: opts.button({
      base,
      dark,
      solid: solid.critical,
      muted: muted.critical,
      mode,
    }),
  }
}

// ../../node_modules/.pnpm/@sanity+ui@1.7.4_react-dom@18.2.0_react-is@18.2.0_react@18.2.0_styled-components@5.3.11/node_modules/@sanity/ui/src/theme/lib/theme/color/button/createButtonModes.ts
function createButtonModes(opts, base, dark, solid, muted) {
  return {
    default: createButtonTones(opts, base, dark, solid, muted, 'default'),
    ghost: createButtonTones(opts, base, dark, solid, muted, 'ghost'),
    bleed: createButtonTones(opts, base, dark, solid, muted, 'bleed'),
  }
}

// ../../node_modules/.pnpm/@sanity+ui@1.7.4_react-dom@18.2.0_react-is@18.2.0_react@18.2.0_styled-components@5.3.11/node_modules/@sanity/ui/src/theme/lib/theme/color/card/createCardStates.ts
function createCardStates(opts, base, dark, name, solid, muted) {
  return {
    enabled: opts.card({
      base,
      dark,
      name,
      state: 'enabled',
      solid,
      muted,
    }),
    disabled: opts.card({
      base,
      dark,
      name,
      state: 'disabled',
      solid,
      muted,
    }),
    hovered: opts.card({
      base,
      dark,
      name,
      state: 'hovered',
      solid,
      muted,
    }),
    pressed: opts.card({
      base,
      dark,
      name,
      state: 'pressed',
      solid,
      muted,
    }),
    selected: opts.card({
      base,
      dark,
      name,
      state: 'selected',
      solid,
      muted,
    }),
  }
}

// ../../node_modules/.pnpm/@sanity+ui@1.7.4_react-dom@18.2.0_react-is@18.2.0_react@18.2.0_styled-components@5.3.11/node_modules/@sanity/ui/src/theme/lib/theme/color/defaults.ts
var black2 = 'hsl(0, 0%, 0%)',
  white2 = 'hsl(0, 0%, 100%)',
  colors = {
    default: {
      lightest: 'hsl(0, 0%, 95%)',
      lighter: 'hsl(0, 0%, 70%)',
      light: 'hsl(0, 0%, 65%)',
      base: 'hsl(0, 0%, 50%)',
      dark: 'hsl(0, 0%, 35%)',
      darker: 'hsl(0, 0%, 20%)',
      darkest: 'hsl(0, 0%, 5%)',
    },
    transparent: {
      lightest: 'hsl(240, 100%, 95%)',
      lighter: 'hsl(240, 100%, 70%)',
      light: 'hsl(240, 100%, 65%)',
      base: 'hsl(240, 100%, 50%)',
      dark: 'hsl(240, 100%, 35%)',
      darker: 'hsl(240, 100%, 20%)',
      darkest: 'hsl(240, 100%, 5%)',
    },
    primary: {
      lightest: 'hsl(240, 100%, 95%)',
      lighter: 'hsl(240, 100%, 70%)',
      light: 'hsl(240, 100%, 65%)',
      base: 'hsl(240, 100%, 50%)',
      dark: 'hsl(240, 100%, 35%)',
      darker: 'hsl(240, 100%, 20%)',
      darkest: 'hsl(240, 100%, 5%)',
    },
    positive: {
      lightest: 'hsl(120, 100%, 95%)',
      lighter: 'hsl(120, 100%, 70%)',
      light: 'hsl(120, 100%, 65%)',
      base: 'hsl(120, 100%, 50%)',
      dark: 'hsl(120, 100%, 35%)',
      darker: 'hsl(120, 100%, 20%)',
      darkest: 'hsl(120, 100%, 5%)',
    },
    caution: {
      lightest: 'hsl(60, 100%, 95%)',
      lighter: 'hsl(60, 100%, 70%)',
      light: 'hsl(60, 100%, 65%)',
      base: 'hsl(60, 100%, 50%)',
      dark: 'hsl(60, 100%, 35%)',
      darker: 'hsl(60, 100%, 20%)',
      darkest: 'hsl(60, 100%, 5%)',
    },
    critical: {
      lightest: 'hsl(0, 100%, 95%)',
      lighter: 'hsl(0, 100%, 70%)',
      light: 'hsl(0, 100%, 65%)',
      base: 'hsl(0, 100%, 50%)',
      dark: 'hsl(0, 100%, 35%)',
      darker: 'hsl(0, 100%, 20%)',
      darkest: 'hsl(0, 100%, 5%)',
    },
  },
  spots = {
    gray: 'hsl(0, 0%, 50%)',
    red: 'hsl(0, 100%, 50%)',
    orange: 'hsl(30, 100%, 50%)',
    yellow: 'hsl(60, 100%, 50%)',
    green: 'hsl(120, 100%, 50%)',
    cyan: 'hsl(180, 100%, 50%)',
    blue: 'hsl(240, 100%, 50%)',
    purple: 'hsl(270, 100%, 50%)',
    magenta: 'hsl(300, 100%, 50%)',
  },
  tones = {
    transparent: {
      bg: [colors.transparent.darkest, colors.transparent.lightest],
      fg: [colors.transparent.lightest, colors.transparent.darkest],
      border: [colors.transparent.darker, colors.transparent.lighter],
      focusRing: [colors.transparent.base, colors.transparent.base],
    },
    primary: {
      bg: [colors.primary.darkest, colors.primary.lightest],
      fg: [colors.primary.lightest, colors.primary.darkest],
      border: [colors.primary.darker, colors.primary.lighter],
      focusRing: [colors.primary.base, colors.primary.base],
    },
    positive: {
      bg: [colors.positive.darkest, colors.positive.lightest],
      fg: [colors.positive.lightest, colors.positive.darkest],
      border: [colors.positive.darker, colors.positive.lighter],
      focusRing: [colors.positive.base, colors.positive.base],
    },
    caution: {
      bg: [colors.caution.darkest, colors.caution.lightest],
      fg: [colors.caution.lightest, colors.caution.darkest],
      border: [colors.caution.darker, colors.caution.lighter],
      focusRing: [colors.caution.base, colors.caution.base],
    },
    critical: {
      bg: [colors.critical.darkest, colors.critical.lightest],
      fg: [colors.critical.lightest, colors.critical.darkest],
      border: [colors.critical.darker, colors.critical.lighter],
      focusRing: [colors.critical.base, colors.critical.base],
    },
  },
  defaultOpts = {
    base: ({dark, name}) =>
      name === 'default'
        ? {
            bg: dark ? black2 : white2,
            fg: dark ? white2 : black2,
            border: dark ? colors.default.darkest : colors.default.lightest,
            focusRing: colors.primary.base,
            shadow: {
              outline: black2,
              umbra: black2,
              penumbra: black2,
              ambient: black2,
            },
            skeleton: {
              from: dark ? white2 : black2,
              to: dark ? white2 : black2,
            },
          }
        : {
            bg: tones[name].bg[dark ? 0 : 1],
            fg: tones[name].fg[dark ? 0 : 1],
            border: tones[name].border[dark ? 0 : 1],
            focusRing: tones[name].focusRing[dark ? 0 : 1],
            shadow: {
              outline: black2,
              umbra: black2,
              penumbra: black2,
              ambient: black2,
            },
            skeleton: {
              from: dark ? white2 : black2,
              to: dark ? white2 : black2,
            },
          },
    solid: ({base, dark, state, tone}) => {
      let color = colors[tone]
      return state === 'hovered'
        ? {
            bg: dark ? color.light : color.dark,
            bg2: dark ? color.light : color.dark,
            border: dark ? color.lighter : color.darker,
            fg: dark ? color.darkest : color.lightest,
            muted: {
              fg: black2,
            },
            accent: {
              fg: black2,
            },
            link: {
              fg: black2,
            },
            code: {
              bg: black2,
              fg: black2,
            },
            skeleton: base.skeleton,
          }
        : {
            bg: color.base,
            bg2: color.base,
            border: dark ? color.light : color.dark,
            fg: dark ? color.darkest : color.lightest,
            muted: {
              fg: black2,
            },
            accent: {
              fg: black2,
            },
            link: {
              fg: black2,
            },
            code: {
              bg: black2,
              fg: black2,
            },
            skeleton: base.skeleton,
          }
    },
    muted: ({base, dark, state, tone}) => {
      let color = colors[tone]
      return state === 'hovered'
        ? {
            bg: dark ? color.darker : color.lighter,
            bg2: dark ? color.darker : color.lighter,
            border: dark ? color.lighter : color.darker,
            fg: dark ? color.lightest : color.darkest,
            muted: {
              fg: black2,
            },
            accent: {
              fg: black2,
            },
            link: {
              fg: black2,
            },
            code: {
              bg: black2,
              fg: black2,
            },
            skeleton: base.skeleton,
          }
        : {
            bg: dark ? color.darkest : color.lightest,
            bg2: dark ? color.darkest : color.lightest,
            border: dark ? color.darker : color.lighter,
            fg: dark ? color.lighter : color.darker,
            muted: {
              fg: black2,
            },
            accent: {
              fg: black2,
            },
            link: {
              fg: black2,
            },
            code: {
              bg: black2,
              fg: black2,
            },
            skeleton: base.skeleton,
          }
    },
    button: ({base, mode, muted, solid}) =>
      mode === 'bleed'
        ? {
            ...muted,
            enabled: {
              bg: 'transparent',
              bg2: 'transparent',
              fg: muted.enabled.fg,
              border: 'transparent',
              muted: {
                fg: black2,
              },
              accent: {
                fg: black2,
              },
              link: {
                fg: black2,
              },
              code: {
                bg: black2,
                fg: black2,
              },
              skeleton: base.skeleton,
            },
            hovered: {
              bg: muted.enabled.bg,
              bg2: muted.enabled.bg,
              fg: muted.hovered.fg,
              border: 'transparent',
              muted: {
                fg: black2,
              },
              accent: {
                fg: black2,
              },
              link: {
                fg: black2,
              },
              code: {
                bg: black2,
                fg: black2,
              },
              skeleton: base.skeleton,
            },
          }
        : mode === 'ghost'
        ? {
            ...solid,
            enabled: muted.enabled,
          }
        : solid,
    card: ({base}) => ({
      bg: black2,
      bg2: black2,
      fg: black2,
      border: black2,
      muted: {
        fg: black2,
      },
      accent: {
        fg: black2,
      },
      link: {
        fg: black2,
      },
      code: {
        bg: black2,
        fg: black2,
      },
      skeleton: base.skeleton,
    }),
    input: () => ({
      bg: black2,
      fg: black2,
      border: black2,
      placeholder: black2,
    }),
    selectable: ({muted, state, tone}) => muted[tone][state],
    spot: ({key}) => spots[key],
    syntax: () => ({
      atrule: black2,
      attrName: black2,
      attrValue: black2,
      attribute: black2,
      boolean: black2,
      builtin: black2,
      cdata: black2,
      char: black2,
      class: black2,
      className: black2,
      comment: black2,
      constant: black2,
      deleted: black2,
      doctype: black2,
      entity: black2,
      function: black2,
      hexcode: black2,
      id: black2,
      important: black2,
      inserted: black2,
      keyword: black2,
      number: black2,
      operator: black2,
      prolog: black2,
      property: black2,
      pseudoClass: black2,
      pseudoElement: black2,
      punctuation: black2,
      regex: black2,
      selector: black2,
      string: black2,
      symbol: black2,
      tag: black2,
      unit: black2,
      url: black2,
      variable: black2,
    }),
  }

// ../../node_modules/.pnpm/@sanity+ui@1.7.4_react-dom@18.2.0_react-is@18.2.0_react@18.2.0_styled-components@5.3.11/node_modules/@sanity/ui/src/theme/lib/theme/color/input/createInputModes.ts
function createInputModes(opts, base, dark, solid, muted) {
  return {
    default: {
      enabled: opts.input({
        base,
        dark,
        mode: 'default',
        state: 'enabled',
        solid: solid.default,
        muted: muted.default,
      }),
      disabled: opts.input({
        base,
        dark,
        mode: 'default',
        state: 'disabled',
        solid: solid.default,
        muted: muted.default,
      }),
      hovered: opts.input({
        base,
        dark,
        mode: 'default',
        state: 'hovered',
        solid: solid.default,
        muted: muted.default,
      }),
      readOnly: opts.input({
        base,
        dark,
        mode: 'default',
        state: 'readOnly',
        solid: solid.default,
        muted: muted.default,
      }),
    },
    invalid: {
      enabled: opts.input({
        base,
        dark,
        mode: 'invalid',
        state: 'enabled',
        solid: solid.default,
        muted: muted.default,
      }),
      disabled: opts.input({
        base,
        dark,
        mode: 'invalid',
        state: 'disabled',
        solid: solid.default,
        muted: muted.default,
      }),
      hovered: opts.input({
        base,
        dark,
        mode: 'invalid',
        state: 'hovered',
        solid: solid.default,
        muted: muted.default,
      }),
      readOnly: opts.input({
        base,
        dark,
        mode: 'invalid',
        state: 'readOnly',
        solid: solid.default,
        muted: muted.default,
      }),
    },
  }
}

// ../../node_modules/.pnpm/@sanity+ui@1.7.4_react-dom@18.2.0_react-is@18.2.0_react@18.2.0_styled-components@5.3.11/node_modules/@sanity/ui/src/theme/lib/theme/color/muted/createMuted.ts
function createMutedTones(opts, base, dark, name) {
  return {
    default: {
      enabled: opts.muted({base, dark, tone: 'default', name, state: 'enabled'}),
      disabled: opts.muted({base, dark, tone: 'default', name, state: 'disabled'}),
      hovered: opts.muted({base, dark, tone: 'default', name, state: 'hovered'}),
      pressed: opts.muted({base, dark, tone: 'default', name, state: 'pressed'}),
      selected: opts.muted({base, dark, tone: 'default', name, state: 'selected'}),
    },
    transparent: {
      enabled: opts.muted({base, dark, tone: 'transparent', name, state: 'enabled'}),
      disabled: opts.muted({base, dark, tone: 'transparent', name, state: 'disabled'}),
      hovered: opts.muted({base, dark, tone: 'transparent', name, state: 'hovered'}),
      pressed: opts.muted({base, dark, tone: 'transparent', name, state: 'pressed'}),
      selected: opts.muted({base, dark, tone: 'transparent', name, state: 'selected'}),
    },
    primary: {
      enabled: opts.muted({base, dark, tone: 'primary', name, state: 'enabled'}),
      disabled: opts.muted({base, dark, tone: 'primary', name, state: 'disabled'}),
      hovered: opts.muted({base, dark, tone: 'primary', name, state: 'hovered'}),
      pressed: opts.muted({base, dark, tone: 'primary', name, state: 'pressed'}),
      selected: opts.muted({base, dark, tone: 'primary', name, state: 'selected'}),
    },
    positive: {
      enabled: opts.muted({base, dark, tone: 'positive', name, state: 'enabled'}),
      disabled: opts.muted({base, dark, tone: 'positive', name, state: 'disabled'}),
      hovered: opts.muted({base, dark, tone: 'positive', name, state: 'hovered'}),
      pressed: opts.muted({base, dark, tone: 'positive', name, state: 'pressed'}),
      selected: opts.muted({base, dark, tone: 'positive', name, state: 'selected'}),
    },
    caution: {
      enabled: opts.muted({base, dark, tone: 'caution', name, state: 'enabled'}),
      disabled: opts.muted({base, dark, tone: 'caution', name, state: 'disabled'}),
      hovered: opts.muted({base, dark, tone: 'caution', name, state: 'hovered'}),
      pressed: opts.muted({base, dark, tone: 'caution', name, state: 'pressed'}),
      selected: opts.muted({base, dark, tone: 'caution', name, state: 'selected'}),
    },
    critical: {
      enabled: opts.muted({base, dark, tone: 'critical', name, state: 'enabled'}),
      disabled: opts.muted({base, dark, tone: 'critical', name, state: 'disabled'}),
      hovered: opts.muted({base, dark, tone: 'critical', name, state: 'hovered'}),
      pressed: opts.muted({base, dark, tone: 'critical', name, state: 'pressed'}),
      selected: opts.muted({base, dark, tone: 'critical', name, state: 'selected'}),
    },
  }
}

// ../../node_modules/.pnpm/@sanity+ui@1.7.4_react-dom@18.2.0_react-is@18.2.0_react@18.2.0_styled-components@5.3.11/node_modules/@sanity/ui/src/theme/lib/theme/color/selectable/createSelectableTones.ts
function createSelectableTones(opts, base, dark, solid, muted) {
  return {
    default: _createSelectableStates(opts, base, dark, solid, muted, 'default'),
    primary: _createSelectableStates(opts, base, dark, solid, muted, 'primary'),
    positive: _createSelectableStates(opts, base, dark, solid, muted, 'positive'),
    caution: _createSelectableStates(opts, base, dark, solid, muted, 'caution'),
    critical: _createSelectableStates(opts, base, dark, solid, muted, 'critical'),
  }
}
function _createSelectableStates(opts, base, dark, solid, muted, tone) {
  return {
    enabled: opts.selectable({
      base,
      dark,
      solid,
      muted,
      state: 'enabled',
      tone,
    }),
    hovered: opts.selectable({
      base,
      dark,
      solid,
      muted,
      state: 'hovered',
      tone,
    }),
    pressed: opts.selectable({
      base,
      dark,
      solid,
      muted,
      state: 'pressed',
      tone,
    }),
    selected: opts.selectable({
      base,
      dark,
      solid,
      muted,
      state: 'selected',
      tone,
    }),
    disabled: opts.selectable({
      base,
      dark,
      solid,
      muted,
      state: 'disabled',
      tone,
    }),
  }
}

// ../../node_modules/.pnpm/@sanity+ui@1.7.4_react-dom@18.2.0_react-is@18.2.0_react@18.2.0_styled-components@5.3.11/node_modules/@sanity/ui/src/theme/lib/theme/color/solid/createSolidTones.ts
function createSolidTones(opts, base, dark, name) {
  return {
    default: {
      enabled: opts.solid({base, dark, tone: 'default', name, state: 'enabled'}),
      disabled: opts.solid({base, dark, tone: 'default', name, state: 'disabled'}),
      hovered: opts.solid({base, dark, tone: 'default', name, state: 'hovered'}),
      pressed: opts.solid({base, dark, tone: 'default', name, state: 'pressed'}),
      selected: opts.solid({base, dark, tone: 'default', name, state: 'selected'}),
    },
    transparent: {
      enabled: opts.solid({base, dark, tone: 'transparent', name, state: 'enabled'}),
      disabled: opts.solid({base, dark, tone: 'transparent', name, state: 'disabled'}),
      hovered: opts.solid({base, dark, tone: 'transparent', name, state: 'hovered'}),
      pressed: opts.solid({base, dark, tone: 'transparent', name, state: 'pressed'}),
      selected: opts.solid({base, dark, tone: 'transparent', name, state: 'selected'}),
    },
    primary: {
      enabled: opts.solid({base, dark, tone: 'primary', name, state: 'enabled'}),
      disabled: opts.solid({base, dark, tone: 'primary', name, state: 'disabled'}),
      hovered: opts.solid({base, dark, tone: 'primary', name, state: 'hovered'}),
      pressed: opts.solid({base, dark, tone: 'primary', name, state: 'pressed'}),
      selected: opts.solid({base, dark, tone: 'primary', name, state: 'selected'}),
    },
    positive: {
      enabled: opts.solid({base, dark, tone: 'positive', name, state: 'enabled'}),
      disabled: opts.solid({base, dark, tone: 'positive', name, state: 'disabled'}),
      hovered: opts.solid({base, dark, tone: 'positive', name, state: 'hovered'}),
      pressed: opts.solid({base, dark, tone: 'positive', name, state: 'pressed'}),
      selected: opts.solid({base, dark, tone: 'positive', name, state: 'selected'}),
    },
    caution: {
      enabled: opts.solid({base, dark, tone: 'caution', name, state: 'enabled'}),
      disabled: opts.solid({base, dark, tone: 'caution', name, state: 'disabled'}),
      hovered: opts.solid({base, dark, tone: 'caution', name, state: 'hovered'}),
      pressed: opts.solid({base, dark, tone: 'caution', name, state: 'pressed'}),
      selected: opts.solid({base, dark, tone: 'caution', name, state: 'selected'}),
    },
    critical: {
      enabled: opts.solid({base, dark, tone: 'critical', name, state: 'enabled'}),
      disabled: opts.solid({base, dark, tone: 'critical', name, state: 'disabled'}),
      hovered: opts.solid({base, dark, tone: 'critical', name, state: 'hovered'}),
      pressed: opts.solid({base, dark, tone: 'critical', name, state: 'pressed'}),
      selected: opts.solid({base, dark, tone: 'critical', name, state: 'selected'}),
    },
  }
}

// ../../node_modules/.pnpm/@sanity+ui@1.7.4_react-dom@18.2.0_react-is@18.2.0_react@18.2.0_styled-components@5.3.11/node_modules/@sanity/ui/src/theme/lib/theme/color/spot/createSpot.ts
function createSpot(opts, base, dark) {
  return {
    gray: opts.spot({base, dark, key: 'gray'}),
    blue: opts.spot({base, dark, key: 'blue'}),
    purple: opts.spot({base, dark, key: 'purple'}),
    magenta: opts.spot({base, dark, key: 'magenta'}),
    red: opts.spot({base, dark, key: 'red'}),
    orange: opts.spot({base, dark, key: 'orange'}),
    yellow: opts.spot({base, dark, key: 'yellow'}),
    green: opts.spot({base, dark, key: 'green'}),
    cyan: opts.spot({base, dark, key: 'cyan'}),
  }
}

// ../../node_modules/.pnpm/@sanity+ui@1.7.4_react-dom@18.2.0_react-is@18.2.0_react@18.2.0_styled-components@5.3.11/node_modules/@sanity/ui/src/theme/lib/theme/color/factory.ts
function createColorTheme(partialOpts = {}) {
  let builders = {...defaultOpts, ...partialOpts}
  return {
    light: _createColorScheme(builders, !1),
    dark: _createColorScheme(builders, !0),
  }
}
function _createColorScheme(opts, dark) {
  return {
    default: _createColor(opts, dark, 'default'),
    transparent: _createColor(opts, dark, 'transparent'),
    primary: _createColor(opts, dark, 'primary'),
    positive: _createColor(opts, dark, 'positive'),
    caution: _createColor(opts, dark, 'caution'),
    critical: _createColor(opts, dark, 'critical'),
  }
}
function _createColor(opts, dark, name) {
  let base = opts.base({dark, name}),
    solid = createSolidTones(opts, base, dark, name),
    muted = createMutedTones(opts, base, dark, name)
  return {
    base,
    button: createButtonModes(opts, base, dark, solid, muted),
    card: createCardStates(opts, base, dark, name, solid, muted),
    dark,
    input: createInputModes(opts, base, dark, solid, muted),
    selectable: createSelectableTones(opts, base, dark, solid, muted),
    spot: createSpot(opts, base, dark),
    syntax: opts.syntax({base, dark}),
    solid,
    muted,
  }
}

// <stdin>
var hues2 = {
    default: {
      lightest: '#ffffff',
      darkest: '#0f172a',
      mid: '#64748b',
      midPoint: 500,
    },
    primary: {
      lightest: '#fdf4ff',
      darkest: '#701a75',
      mid: '#d946ef',
      midPoint: 500,
    },
    transparent: {
      lightest: '#ffffff',
      darkest: '#111826',
      mid: '#6b7180',
      midPoint: 500,
    },
    positive: {
      lightest: '#f8fafc',
      darkest: '#0f172a',
      mid: '#43d675',
      midPoint: 400,
    },
    caution: {
      lightest: '#fffbeb',
      darkest: '#783510',
      mid: '#f59e09',
      midPoint: 300,
    },
    critical: {
      lightest: '#fef1f2',
      darkest: '#881337',
      mid: '#f43f5e',
      midPoint: 500,
    },
  },
  createTheme = (_hues) =>
    themeFromHues({
      createColorTheme,
      hues: _hues,
      multiply,
      parseColor,
      rgba: rgba2,
      rgbToHex,
      screen,
      studioTheme,
    }),
  theme = createTheme(hues2)
export {createTheme, hues2 as hues, theme}
