import {example1SchemaType} from './example1'
import {validationTest} from './async-functions/schemaType'
import formComponents from './formComponents'

export const v3docs = {
  types: [example1SchemaType, validationTest, formComponents],
}
