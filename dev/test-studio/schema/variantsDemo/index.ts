/**
 * Re-export coffee shop schema types under the legacy variantsDemo path
 * so existing imports keep working.
 */
export {
  coffeeShopSchemaTypes,
  demoCoffeeLandingPage,
  demoCoffeeOrigin,
  demoCoffeeProduct,
  demoCoffeePromo,
  variantsDemoTypes,
} from '../coffeeShop'
