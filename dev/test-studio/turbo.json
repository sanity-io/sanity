{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["ENABLE_VITE_DEVTOOLS", "REACT_PRODUCTION_PROFILING"],
      "outputs": [".sanity/**", "dist/**"]
    },
    "start": {
      "dependsOn": ["build"],
      "cache": false,
      "persistent": true
    }
  }
}
